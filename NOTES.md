# 既知の問題・あとで直すこと

## 目次

- [未着手の課題](#未着手の課題)
- [進行中: 床埋め後の大ツモ・梯子](#進行中-床埋め後の大ツモ梯子)
- [測定のしかた](#測定のしかた何度も踏んでいる罠) ← 数字を出す前に読む
- [調査済み: 終盤の低段位散在による即死](#調査済み-終盤の低段位散在による即死)
- [調査済み: 盤が崩れる仕組みと段階の切り分け](#調査済み-盤が崩れる仕組みと段階の切り分け-2026-08-18)
- [着手タイミング](#着手タイミング)
- [方策 (bootstrap) の設計](#方策-bootstrap-の設計)
- [学習まわり](#学習まわり)
- [予定: RL (REINFORCE)](#予定-rl-reinforce)

## 未着手の課題

**ビジョン**
- `10.png`: 同系色マスク融合＋枠外イチゴ。切り出し再設計が必要で、方策／held よりコスパが悪い

**物理シミュレーション**
- フルーツ同士の摩擦が低そう: 本家よりやたらとフルーツが滑り込む。

**学習パイプライン**
- 学習エピソード長: `max_steps` を増やし、`episodes` を減らす（長い対局を少なめに）。
  `train_sim.py` の既定は 300（実測の自然終了は中央 210 手・最大 311 手なので、
  打ち切りは 2% ほど。0% にしたいなら 320）

## 進行中: 床埋め後の大ツモ・梯子

床が埋まったあと、オレンジ／デコポンなど大きいツモを小さい側へ置いて下の実を潰し崩れる問題。
`_packed_small_side_penalty`（`src/policy.py`）で対応。**常時有効**（トグルは削除済み）。

### n=100 で測った結果 (2026-08-06)、有意差は付かなかった

`--episodes 100 --max-steps 400`（打ち切り 0 本、2.6 時間）:

- score 2047.0 → 2093.9（**+46.9**、t=0.85、95% CI **[-62.3, +156.1]**）。**有意でない**
- シード対戦 win 52 / loss 38 / tie 10。tie が 10 なので、発火機会自体は 90 本ある
- **分位で見ると下位だけ上がっている**（min 1045→1315、下位10本平均 1318→1450）。
  上位は動かない（上位10本平均 2914→2913、type10 到達 14→17 本）。
  床埋め後の崩れを防ぐルールなので狙いとは一致するが、**下位を後から選んで
  検定するのは事後選択**。次は「score<1500 の本数」のような閾値を先に決めて測る
- この +46.9 を有意にするには **n=529（約 14 時間）**。割に合わないので測り直さず、
  **ON で恒久化した**（点推定は正、狙いと形が一致、発火 90/100）。A/B 用のトグル
  （`SUIKA_PACKED` / `set_packed_rule_enabled`）は削除済み

角の大実を階段状に発火させる「梯子」（角桃の内側の隣に梨、その2つの**肩**にリンゴとオレンジ、
最後のオレンジで発火して 4→5→6→7 と連鎖）は、この置き分けルールの結果として自然に出てくる形。
`src/ladder.py` の `find_anchor` / `rungs` で検出だけ書いてあるが、**本番の経路からは一度も呼ばれていない**
（呼んでいるのは `tests/test_policy.py` だけ）。手選びに使うときの土台として残してある。
段数をそのまま報酬にする形は測って見送った
（→[梯子の段数ボーナス](#試して見送った-梯子の段数ボーナス-2026-08-19)）。

### 分かっていること

- **発火に誘導は不要**。梯子が組み上がっていれば `choose_x` は x を全振りした最良と同点を取る。
  発火用の候補 x を足しても no-op だった（削除済み）
- **`FOREIGN_AIM` は梯子と無関係**。段は肩に乗るので、そもそもこの減点が掛からない。
  真上で安定することが物理的にない
- **`_size_order_penalty` も邪魔していない**。梯子の盤で実測 0.14〜0.49 しかない
- **床が埋まっていないと形が保たない**。梨が楔で押し出されて自壊し、どこに落としても
  1 段ぶん（15点）しか取れない。床を右端まで埋めると 100 点。床の詰まりはゲート条件
- **ボトルネックは組み立て**。4段揃った盤は 720 中 12 回しか出ない。段を「落として置く手」
  として条件を書くのは筋が悪い（ツモれるのは orange まで。梨・リンゴは合成でしか作れない）
- 小側の room 判定は幾何の隙間幅だけでなく、実際に `simulate_drop` で確認する
  （`_small_side_room_ok`）。屋根で塞がれた隙間を room ありと誤判定するバグを直した

### 試して見送った: 梯子の段数ボーナス (2026-08-19)

「組み上がった段数 × w を `_evaluate_drop` の減点から引く」を実装し、単独
スクリーニングに掛けた。**A/B に上げずに revert した**（実装は残していない）。

**足切りの条件は測る前に固定した**: ① どれかの w で手の変化率 3% 以上
② 着地後の段数分布が上向きに動く ③ 段 3 以上の局面で合成を取る割合が
w=0 から落ちない（発火抑制の見張り）。

**局面集合**: 6 seed を通し打ちして 60 手目以降を 1 手おきに取った 428 局面。
最初に 1〜60 手目で取った集合は段 0 が 87%、段 3 以上が 4 件しかなく、
機構が噛む前の盤しか見ていなかった。**梯子は床が詰まってから組み上がる形なので、
序盤の局面集合では何も測れない**（1 回目のスクリーニングはこれで無駄になった）。

| w | 手の変化率 | 着地後の段数 0/1/2/3/4+ | 段3以上で合成を取った手 |
|---|---|---|---|
| 0 | 0/428 (0.0%) | 296/36/28/27/41 | 3/66 |
| 5 | 5/428 (1.2%) | 294/37/29/26/42 | 3/66 |
| 10 | 6/428 (1.4%) | 294/36/29/27/42 | 2/66 |
| 20 | 7/428 (1.6%) | 293/37/28/27/43 | 2/66 |
| 40 | 12/428 (2.8%) | 291/37/30/26/44 | 2/66 |
| 80 | 17/428 (4.0%) | 290/37/30/24/47 | **0/66** |

**噛む w と壊さない w が重ならない。** ①を満たすのは w=80 だけで、そこでは③が
全滅する。梯子が発火すると段が 4→0 に落ちるので、発火手だけが −4w を背負う。
梯子 1 本は 100 点なので、w=20 でも取り分が実質 +20 まで削れ、w=80 では取れなくなる。
**この項は梯子を組むことと発火することを取り引きしていて、点があるのは発火の側**。

**効かない理由は重みではなく形にある。** 同じ 428 局面で「全候補のうち段数を最大
どこまで伸ばせるか」を数えると、**91.6% の局面が +0 段**（+1 段が 29 件、+2 段が 7 件）。
段数ボーナスは落としたあとの盤に付くので、伸ばせる候補が無い局面ではどの候補も
同じ値を取り、定数差として消える。**天井が 8.4%** で、w=20 が実際に動かしたのは 1.6%。
材料がツモれないことは先に分かっていた（上の「ボトルネックは組み立て」）。
段を「落として置く手」として条件を書くのが筋が悪いのと同じ理由で、
**「落としたあとの段数」に報酬を付けても同じ壁に当たる**。

**score / cascades は測っていない。** 足切りで落ちたので A/B は回していない。
1.6% の手の変化は n=25 では score のノイズに埋もれる。

**1 局でスイカに届いたことは根拠にならない。** w=20 で seed 74546 がスイカに
到達したという観測が発端だったが、手が 1 つ変われば盤はそこから完全に分岐するので、
1 局の到達段位はツモ運と区別できない（→[測定のしかた](#測定のしかた何度も踏んでいる罠)）。

**次にやるなら形を変える。** 段数そのものではなく「梯子が組める余地」
（土台が壁に付いていて内側が空いているか）を見るか、段の材料を合成で作る手筋を
引く側に報酬を付ける。落としたあとの段数を数える形では動かせる局面が足りない。

### 調査済み: 同点プラトーは広いが、閉じ込めはその中で動かない (2026-08-19)

08-18 の調査で「全 33 候補が幅 0.09 の同点プラトーに収まり、いちごを閉じ込める手が
閉じ込めない 13 本を eval 0.07 差で上回った」という 1 局面が出ていた。これが
どれくらいの頻度で起きているかを 428 局面（60 手目以降、6 seed）で数えた。

閉じ込めの定義は「より大きい実に左右から挟まれ、同種の相方も残っていない実」
（`_is_nestled` をそのまま使った）。**閉じ込め数は eval に入っていない**ので、
帯の中でこれがばらつくなら eval が見えていない差ということになる。

| eps | 帯の候補数 中央/最大 | 閉じ込めに差がある局面 | 選んだ手が最小より悪い | 超過 | 要る eval 差 中央 |
|---|---|---|---|---|---|
| 0.1 | 9 / 38 | 5/428 (1.2%) | 5/428 (1.2%) | 1.00 | 0.004 |
| 0.5 | 11 / 39 | 10/428 (2.3%) | 7/428 (1.6%) | 1.00 | 0.006 |
| 2.0 | 14 / 39 | 20/428 (4.7%) | 14/428 (3.3%) | 1.00 | 0.431 |

候補数は中央 43 本。

**プラトーは常態。** 中央 43 候補のうち 9 本が eval 0.1 以内に並ぶ。
08-18 の「幅 0.09 に 33 候補」は特異な局面ではない。

**ただし閉じ込めはその帯の中でほとんど動かない。** 差が出るのは 1.2%（eps=0.1）、
帯を eps=2.0 まで緩めても 4.7% しかない。残りでは帯のどの手を選んでも閉じ込め数が同じ。
つまり**閉じ込め減点は帯の中で定数になり、手を選べない**。
[梯子の段数ボーナス](#試して見送った-梯子の段数ボーナス-2026-08-19)と同じ失敗の形で、
重みの問題ではなく「差を作れる局面が無い」という形の問題。

**08-18 に閉じ込め減点が効かなかった理由はこれ。** あのときの A/B は
「3 つ全部 / ゲートだけ / 縦だけ」の 3 変種で、**閉じ込め減点だけを単独で測ってはいない**
（一番負けたのは縦だけの −8.5%）。単独でも効かないことがここで分かる。

**単独 A/B は推奨しない。** 拾える局面が 1.2〜3.3% で、この幅は n=25 では
score のノイズに埋もれる。必要な eval 差が 0.004 と極小なのも同じことを言っていて、
「ほんの少し足せば拾えるが、拾える局面がほとんど無い」形。

#### 帯の中では、大きい項がすべて飽和している (同日)

「では帯の中で何が動いているのか」を、eval を項ごとに分解して測った
（分解が `_held_eval` と一致することは局面ごとに突き合わせて確認）。
帯 (eps=0.1、中央 9 候補) の中の幅:

| 項 | 幅 中央 | 幅 最大 | 幅>0.01 の局面 |
|---|---|---|---|
| variance (凸凹) | 0.009 | 2.972 | 168/375 (44.8%) |
| big_layout | 0.000 | 0.330 | 66/375 (17.6%) |
| danger | 0.000 | 2.973 | 31/375 (8.3%) |
| size_order | 0.000 | 0.034 | 8/375 (2.1%) |
| bury | 0.000 | 0.000 | **0/375 (0.0%)** |
| foreign_aim | 0.000 | 0.000 | **0/375 (0.0%)** |
| packed | 0.000 | 0.000 | **0/375 (0.0%)** |
| score / excess_same / valley_grow | 0.000 | 21 / 20 / 3 | 0.3〜0.5% |

**幅ゼロは「死んでいる」からではない。** 帯に限らず全候補で絶対値を測ると
（60 局面 / 2660 候補）、bury は 41.9% の候補で非ゼロ・最小 −80.0、
foreign_aim は 18.6%・最小 −100.0、size_order は 70.3%・最小 −200.1、
excess_same は 69.8%・最小 −280.0。**大きく効いている**。

**これらの項は「悪い候補を帯の外へ叩き出す」仕事をしていて、帯に残った候補は
既に全部その項で同点になっている。** 大きい項はどれも「個数 × 重み」の離散量なので
飽和する。その解像度より下では eval に言うことが無く、決定は 1/1000 の
スケールの凸凹項（中央 0.009）に落ちる。

**帯は 3 割が候補刻みの artifact、7 割は本物。** 帯の中の相異なる着地結果を
数えると、候補 中央 9 本 → 相異なる盤 中央 2 通り。
**31.8% の局面は帯が 1 通りの盤に潰れる**（`CANDIDATE_STEP = 12.0` が物理の
解像度より細かいだけで、どれを選んでも同じ手）。残る **68.2% は本当に別の盤を
2 通り以上含み、22.0% は 5 通り以上**。つまり終盤の手の約 2/3 で、
方策は実質的に別々の未来をでたらめに選んでいる。

**カウント型の項をもう 1 つ足しても同じ壁に当たる。** 梯子の段数も閉じ込め数も
同じ種類の離散量なので、帯の中でやはり飽和して動かない
（→[梯子の段数ボーナス](#試して見送った-梯子の段数ボーナス-2026-08-19)、上の閉じ込めの表）。
**今日 2 つ潰れたのは偶然ではなく、同じ形の失敗**。帯を割るには
着地位置に対して連続に変わる量か、飽和しない評価が要る。
bootstrap を「RL 前の薄い方策」として置いてある前提
（→[方策 (bootstrap) の設計](#方策-bootstrap-の設計)）は、この測定と整合する。

## 測定のしかた（何度も踏んでいる罠）

**score のノイズが非常に大きい。** これが方策改善の一番の壁で、下の試行はすべて
ここに埋もれた。数字を出す前にこの節を読むこと。

- 個体差の標準偏差は ~1000〜1200、同一シードのペア比較でも差の標準偏差は 78〜496。
  ±100 点を有意に見るには **n≈100** 要る。数十エピソードでは足りない。
  score より低分散な代理指標（生存手数、特定局面での孤立実の個数など）を探す方が早い
- **平均の増減だけで判断しない。** `compare_policy.py` が指標ごとにペア差の t 値と
  95% CI を出す（`src/util/stats.py`）。CI が 0 をまたぐ行はその n では何も言えていない。
  有意でないときは「±100 点を語るのに必要な n」も表示する
- 長い実行には `--out artifacts/xxx.json` を付けてシードごとの生データを残す。
  集計より先に書き出すので、集計側の不具合で数時間ぶんを失わない
- 代理指標の選定は `scripts/analyze_ab.py <dump>`。指標を n_detect（その差を
  0 から離すのに要るエピソード数）で並べる。単位に依らないので score と生存手数を
  直接比べられる。ただし**同じダンプで一番良く見えた指標を採るのは選定バイアス**。
  別の変更・別シードで撮った 2 本目でも上位かを確認してから採用する
- **代理指標は `cascades`（1 手 3 合成以上の回数）で確定した (2026-08-17)。**
  独立な 3 ダンプ（packed / valley_grow / wider_lookahead）すべてで上位に来て、
  感度比 1.34 / 1.28 / 1.40、r(score) 0.83 で安定している。wider_lookahead では
  **score が有意にならなかった差を cascades だけが検出した**（t=2.36）。
  他の候補は落ちる: `early_crown` は packed で 1 位だが r≒-0.05 で score と無関係、
  `steps` / `merges` は r=0.97 と高いが感度が score とほぼ同じで得がない。
  n を減らす効果は感度比ぶん（1.4 倍なら必要 n が約半分）で、万能ではない
- **打ち切りが出たら数字を割り引く。** 打ち切られるのは伸びた対局なので、
  良い変更ほど過小評価される。自然終了は**実測 200 本で平均 213 / 中央 210 / 最大 311 手**
  （旧記述の「300〜400 手」は好条件の 1 局から来た過大な値）。
  `--max-steps` は **320 以上で打ち切り率 0%**、既定は 400。200 だと 64%、
  **100 だと 100% が打ち切り**（08-03 の事故はこれ）。`compare_policy.py` は
  1 本でも打ち切られたら警告する
- **出力で層別して同じ出力を比べない（平均への回帰）。** A のスコアで上位/下位に
  分けて A と B を比べると「上位が悪化・下位が改善」が必ず出る。B のスコアで
  分ければ鏡像になる。実際 `SUIKA_PACKED` の n=100 でこれを踏みかけた。
  分布を比べたいなら分位点どうしを直接比べる
- シードは固定しない（`--seed` 省略でランダム）。固定シードの使い回しは、たまたまの崩れを
  「再現した」と誤読しやすい。変更の比較は同一シードでペアにする

測る手順そのもの（A/B の差し方は [AGENTS.md](AGENTS.md#方策学習を触るとき)、
作業ツリーを汚さない比較は [AGENTS.md の git](AGENTS.md#git)）はあちらに置く。
ここは何を信じてよいかだけを書く。

## 調査済み: 終盤の低段位散在による即死

3500 点前後を狙って bootstrap を強化しようとした際の調査記録。

### 死因診断

eval_policy.py で計測すると、24〜40 エピソード規模で score 平均は 1900〜2150 点
（NOTES 冒頭の「1 局 311 手・score 3305」は好条件の一例で、平均的な結果ではない）。
全エピソードが `dead` で終了し、`max_steps` 打ち切りには一度も届かない。

1 エピソードを手ごとに再生して追跡したところ（seed=20260816）、死亡直前の 10 手以上、
cherry 5〜8 個・grape 3 個・dekopon 4 個など低段位の実が合成されないまま盤面全体に
散らばって残っていた。最終手（オレンジ）は安全な着地先が無く、既に危険な高さだった
右上のタワーへ強制着地して即死。さらにその手の直前局面で cherry を x=0〜400 の
どこに落としても合成が一切成立しないことを確認した。つまり「その手」の問題ではなく、
もっと前の手で低段位の実が大きい異種に側面から挟まれ、物理的に合体不能な位置まで
散らばってしまっていたことが根本原因。

### 改善試行 (いずれも改善を確認できず・コードは revert 済み)

| 試行 | 内容 | 結果 |
|---|---|---|
| 同種過多の三角数化 | `_excess_same_penalty` を線形 (超過1個=20) から三角数 (1個超過=1x, 2個=3x…) へ | n=24 で +1.9% / n=40 で -3.0% と結果が割れる |
| 側面隔離ペナルティ | 相方のない実をこの手で左右とも塞ぐ手を検出する `_isolation_penalty` を新設 | n=32 で -9.2% (SE~206 に対し差 195) |
| 3 手先読み | eval 上位 `NEXT_TOP=2` 本だけ 3 手目を展開。型未知なので 5 型の `ideal_x` 1 点平均で近似、`THIRD_PLY_DISCOUNT=0.4`。1 手 313→401ms | n=32 で -0.7% |

対症療法 (重み調整・新規ペナルティ・先読みを深くする) は 3 回とも noise floor に
埋もれた。RL 側も [BC が match 60-70% に届かない](#調査済み-bc-が-match-60-70-に届かない-2026-08-05)
で頭打ちが判明しており、bootstrap の微調整・浅い先読みの延長では 3500 点への
見通しが立っていない。次に手を出すなら 100+ エピソードでの再検証か、
学習方策の特徴量・アーキテクチャ自体の作り直しのような質的に異なる変更が必要。

### 調査済み: eval の加点側は律速ではない (2026-08-17)

**仮説**（外れた）: `eval = score - penalties` の加点が本家点そのものなので、
cherry 合体は 1 点・grape 合成でも 3 点にしかならない。一方で減点は `EXCESS_SAME`
が 20、`FOREIGN_AIM` が 100 のオーダー。だから小さい実を片付ける動機が構造的に
埋もれていて、上の「低段位散在による即死」を招いているのではないか。

**試したもの**: 盤の実の個数そのものを減点にする `FRAGMENT_WEIGHT`
（`board_penalties` に `w * len(fruits)`）。合体 1 回で個数が 1 減るので、
これは「型に依らない合体ボーナス」と数学的に同じ形になる。落下で必ず 1 個
増えるぶんは候補間で共通なので手の選択には効かず、先読みの次手にも同じ形で乗る。
個別のペナルティ規則を足すのではなく、加点側の形を変える変更として試した。

**A/B を回す前に落ちた。** 一致率スクリーニング（3 シード × 60 手 = 180 局面）で、
現行と同じ x を選ぶ割合が **w=3〜15 で 98.9〜98.3%、w=25 でも 94.4%**。
ほぼ no-op なので score では測っていない（数時間を賭ける価値がないと判断）。

**なぜ効かないのか（候補を間引かずに数え直した。3 シード × 90 手 = 270 手）**:

| | |
|---|---|
| 合体した手 | 138 (51%) |
| 合体できる x があったのに見送った手 | **6 (2%)** |
| そもそもどこにも合体が無い手 | 126 (47%) |

**合体可能な局面の 96% で、現行方策は既に合体を取っている。** 加点が 1 点でも、
合体すれば実が 1 個減って埋め込み・同種過多・高さの減点がまとめて外れるので、
**減点の解除が加点の代わりをしていた**。加点側を厚くしても足す先が無い。

**死因の見方も更新する。** 進行別に測ると（3 局 575 手）、「合体できる手」の割合は
序盤 47% → 終盤 57% で**終盤に落ちない**（実際の合体率もそれに追随する）。
一方で盤の実数は 4.3 → 17.0 と増え続ける。つまり終盤の崩壊は
「**合体できなくなる**」のではなく「**合体しても供給に追いつかない**」。
1 手で必ず 1 個増えるのに対し、単発の合体は 1 個減らすだけで、複数減らせるのは
連鎖だけ。[代理指標](#測定のしかた何度も踏んでいる罠)で `cascades` が
一貫して最も鋭いのも、これと整合する。手を入れるなら**連鎖の起こしやすさ**を
狙う方が筋が良さそう。

## 調査済み: 盤が崩れる仕組みと段階の切り分け (2026-08-18)

6 局を手ごとにトレースして数えた（局面単位の決定的な量なので、score の
ノイズには載っていない）。「盤面を崩さないことがスコアより重要」という
方針が、減点の重み付けでは逆になっていた。

### 症状

- **横の大小順の逆転率は序盤 10% → 終盤 40〜50%**。50% は完全に無秩序
- **縦は最初から無法地帯**。縦に重なった 23079 組のうち 47% が「上のほうが大きい」。
  `_size_order_penalty` は `a.x <= b.x` しか見ておらず、縦の規則が 1 つも無かった
- cherry / strawberry が主犯（1 手あたり +1.25 / +1.57 ペア）。orange だけが −0.93 で回収
- **綺麗な手は候補に入っている。評価が蹴っている**: cherry は汚さない手が
  候補にある局面が 97% あるのに、実際に選ぶのは 64%。1 手あたり 2.85 ペアの取りこぼし

### 何が大小順に勝っていたか

綺麗な手を蹴った局面で項ごとに（選んだ手 − 綺麗な手）を取ると:

| 項 | 差 | 決め手になった率 |
|---|---|---|
| `bury_block` | **−7.43** | 35% |
| `FOREIGN_AIM` | −6.12 | 6% |
| 谷育成 | −1.04 | 35% |
| `sizeord` | −0.03 | 16% |

**`sizeord` は汚い手と綺麗な手を実質区別していない。** 換算すると逆転 1 ペア
≒ eval 4.91 に対し、`bury_block` は型差 1 で 14.0、`FOREIGN_AIM` は 100.0。

- `bury_block` は小側候補の 51〜72%（straw/grape/orange）で発火し、
  **守っているペアの 66% は既に大きい実が間に挟まって合体不能**。
  83% は接触距離の 3 倍以上離れている。中央値 28.0 / 最大 133.0 を、
  もう死んでいるペアのために払っていた
- 谷育成は発火 1642 件の **100% が大側への着地**。97% は「谷に同種の実が
  あるが、この手では合体しない」＝その実の隣に積む手だった
- `_size_order_exempt` の免除は従犯。実 16 個以上の盤で小さい実の 45〜57% が
  免除対象になるが、免除を全部外しても 1 ペアあたり 4.91 → 6.20 にしかならない

### スクリーニングは 3 つとも「良い」と言った（166〜230 局面、決定的）

**この節は「なぜ採用してしまったか」の記録。**A/B の結果は次節。同一局面で
候補どうしを比べる決定的な量なので noise は無いが、**score との対応が無い**。

| 変種 | 一致率 | 横の逆転 | 縦の逆転 |
|---|---|---|---|
| 縦 1.5 / ゲート無し | 89.8% | −0.24 | −1.06 |
| 縦 1.5 / ゲート 0.35 | 86.1% | −0.52 | −0.43 |
| 縦 3.0 / ゲート無し | 88.0% | **+0.15** | −0.95 |

閉じ込め減点は重み 0/2/4/8 で全閉込 −9/−18/−20/−27、逆転 +34/+30/+26/+28、
合体 235/235/234/233。どれも「閉じ込めも逆転も減って合体は不変」と読めた。

ここから決めた値（縦 1.5、ゲート 0.35、閉じ込め 4.0）が、A/B では全部負けた。

**副産物として拾えた事実**（規則を戻しても残る）:

- 逆転率はペアの左右だけで数えると足りない。オレンジ・ぶどう・りんご の並びでは
  ぶどうが逆転するのは オレンジ とだけで 1/3 = 0.333 にしかならないが、
  大きい実 2 つに挟まれた実は明らかに崩れた形。**谷にいる実は左右どちらの壁に対しても
  外している**と数えるべき（`_valley_flanks` を再利用すると 2/3 = 0.667）。
  この誤りは `test_grows_valley_fruit_when_held_and_next_are_one_smaller` が
  検出していた。**テストが正しく、指標のほうが間違っていた**
- 谷の有無だけを盤の状態の判定に使うのは駄目。実 8 個以上では必ず谷があるので
  常に真になる。谷込みの逆転率なら実数別に 98% / 70% / 48% / 16% / 3%
  （実 0-3 / 4-7 / 8-11 / 12-15 / 16+）と勾配が残る
- `_floor_packed` は**実 3〜5 個の盤の 48%、6〜8 個の 74% を「埋まっている」と読む**。
  「床が詰まって小側に置き場が無い」という `packed_small_side_penalty` の前提が
  序盤から成立していない（→[床の埋まり判定](#決着-床の埋まり判定はツモ基準-2026-08-19) で直した）

### 決着: 床の埋まり判定はツモ基準 (2026-08-19)

`_floor_packed` の閾値をオレンジ直径の固定値からツモの直径に変えた。

`packed_small_side_penalty` は `_floor_packed` の唯一の呼び元で、
`PACKED_BIG_DRAW_MIN_TYPE = SPAWN_MAX_TYPE - 1` により**デコポンとオレンジの
2 種類でしか走らない**。旧閾値はオレンジ直径 77.1 の固定値だったので、
オレンジ側は元から正しく、ずれていたのはデコポン（直径 59.6）だけだった。
デコポンが 17.5 の余裕をもって落ちる隙間まで「置き場が無い」と読んでいた。

- 変更前の埋まり判定率（8 seed × 40 手）は実 3〜5 個で **43.8%**、6〜8 個で **89.5%**。
  一列に並んだだけの盤（`pear@70 grape@216 straw@288 cherry@346`、最大隙間 67.1）が
  デコポンのツモに対して埋まり扱いだった
- 旧新の述語を差し替えて同じ局面で `choose_x` を両方回すと（6 seed × 60 手）、
  **356/360 手が一致（98.9%）で 4 手だけ変化**。減点の発火は 1445 → 1219（**−15.6%**）
- **A/B は回していない。** 1.1% の手の変化は n=25 では score のノイズに埋もれる
  （→[測定のしかた](#測定のしかた何度も踏んでいる罠)）。前提の誤りを直す変更で、
  点数を動かす主張はしていない

### 試して戻した: 縦の大小順・段階ゲート・閉じ込め減点（2026-08-18）

**3 つとも A/B で負けたので revert した。** 作った経緯と、なぜ外したのかを残す。

**きっかけ**: view_sim で明らかに汚い盤が出たという報告。桃-オレンジ-桃 に崩れた。

**因果はこうだった。** 崩壊の起点は 13 手前:

- **25 手目**: 盤は `peach@70 orange@172 straw@230 cherry@384` の完全な降順
  （逆転率 0.000・閉じ込め 0）。デコポンを落とすとき、いちごを閉じ込める手が、
  閉じ込めない **13 本**の候補を **eval 0.07 差**で上回った。全 33 候補が
  −6.12〜−6.21 の幅 0.09 に収まる同点プラトー
- **28 手目**: その閉じ込めのせいで いちご と オレンジ の位置が入れ替わり、
  **もうどこに置いても悪い**。ideal_x に置いても局所の逆転量が同じ 3 単位で区別できない
- **38 手目**: 64 点 3 連鎖。x=204〜276 のどこでも同じ結果で、連鎖を取らない手は
  閉じ込めが 2→3 に増える。**取るのが正しい**。その結果できた桃が オレンジ の右に出て確定

**桃-オレンジ-桃 は結果であって原因ではない。**

**外れた見立て（記録として残す）**:

- `packed_small_side_penalty` が犯人 → 違う。28 手目で 8.0 を足してはいるが、
  外しても選択は変わらない。241 局面で試して**一致率 100%**、この規則は手の選択に
  影響していない（ただし `_floor_packed` が実 3〜5 個の盤で 48% を「埋まっている」と
  読むのは別の問題で、こちらは
  [床の埋まり判定](#決着-床の埋まり判定はツモ基準-2026-08-19) で直した）
- 局所 ordering 項（落とした実が作る逆転を手ごとに数える）が効く → 25 手目でも
  28 手目でも w=4.0 まで振って変化なし。着地が右でも左でも逆転する相手が同じで、
  **逆転の数では区別がつかない**

**作ったもの（すべて revert 済み）**

| 規則 | 内容 |
|---|---|
| 縦の大小順 | 横に重なって縦に積まれた組で、上のほうが大きいとき型差 × 1.5 |
| 段階ゲート | 逆転率がしきい値を超えた盤でだけ、立て直し側の規則（bury_block / 谷育成）を掛ける |
| 閉じ込め減点 | より大きい実に左右から挟まれ、同種の相方も残っていない実に 1 個 4.0 |

**A/B（n=25、同一シード、`--seed 526304`）。全部マイナスだった**:

| 変種 | score | Δ | t | cascades | max_type |
|---|---|---|---|---|---|
| 3 つ全部 | 2045.60 | −5.5% | −1.04 | −7.9% | −1.3% |
| ゲートだけ | 2035.24 | −6.0% | −1.05 | −3.1% | −1.7% |
| 縦だけ | 1981.20 | **−8.5%** | −1.73 | −10.8% | — |
| 閉じ込めだけ | 2077.04 | −4.1% | −0.73 | −5.0% | −1.3% |

A 側はどれも 2165.44（3 規則を切った状態）。有意なものは 1 つも無い（±100 点には
n≈127 要る）が、**4 本すべて・全指標が負**。悪化シードは軒並み max_type 10 → 9 で、
ダブルスイカの目標に一番近い指標がはっきり落ちた。

**一番の教訓: 自作の構造指標が 3 回とも逆を指した。**
閉じ込め数・逆転数で足切りして重みを決め、3 つとも「良い」と出たのに score は全部
下がった。[測定のしかた](#測定のしかた何度も踏んでいる罠)の選定バイアスの項そのもの。
**score で検証していない代理指標を、重み決めの根拠に使ってはいけない。**
使ってよいのは既に検証済みの `cascades` だけ。

**個別に分かったこと**:

- **段階ゲートは論理的に成立しない。** 谷は「自分より大きい実に左右から挟まれた」形
  なので、**小さい側に大きい実がある時点でそれ自体が並び順違反**。つまり
  *谷があるなら逆転は必ずある*（実測 332 盤すべてで逆転率 > 0）。だから
  「谷育成したいが盤は整っている」という状態は存在せず、ゲートはしきい値 0 なら
  完全な no-op、0 より上なら**壊れた盤で立て直しを禁止する**害だけが出る。
  実測でも、谷育成が成立した 151 局面のうち 28 件（19%）を止めており、
  その 28 件は最小でも逆転率 0.174 だった
- **縦の大小順は前提が疑わしい。** 「縦に重なったペアの 47% が逆さ＝無秩序＝欠陥」と
  読んだが、**合体すると大きい実がその場に生まれる**以上、小さい実の上に大きい実が
  乗るのはこのゲームの正常な姿の可能性がある。横の逆転は seed=74546 で崩壊まで
  追えたが、縦は横からの類推だけで規則にした。単独 −8.5% が一番悪い
- **閉じ込め減点は狙いの手は直した**（25 手目の悪手は消えた）が、それでも −4.1%。
  相方のいる実まで数えると更に悪く、seed=74546 が 241 手 → 163 手に落ちた
  （谷に餌を入れる手を潰していた）

**注意**: 4 本とも同じ 25 シードなので独立な証拠ではない。別のシード集合で
確認したわけではない。ただし seed=74546 の通し打ちも独立に負を指していた。

### `bury_block` は廃止した（2026-08-18）

`bury_block_penalty` は**どちらの構造指標でも効果が検出できなかった**ので削除した。
（当時は縦の大小順とゲートが入った状態で測ったが、それらは後に revert したので、
**この廃止だけが未測定のまま残っている**。master と単独で A/B に掛ける必要がある。）
構造的にも「小さい実の上に大きい実」の型差減点で
（`14.0 ×(drop_type - under.type)` は「小さい実の上に大きい実」の型差減点そのもの）、
「下の実に相方がいる」条件と 9 倍の重みが付いていただけだった。

166 局面で、消すと手は 10% 変わる（一致率 89.8%）。そのうえで:

| 変種 | 盤の汚れ（横＋縦の逆転）合計 | 生きた合体ペアの増減 | 合体回数 |
|---|---|---|---|
| 現行 | +40 | −2 | 168 |
| **廃止** | **+40** | **±0** | 168 |
| 生きたペアだけに絞る | — | −1 | 168 |

汚れは差し引きちょうど 0（変わった 17 手の増減が完全に相殺した）。**本来の仕事である
合体ペアの保護でも、消したほうがわずかに多く残る。** 合体回数は 3 本とも同一。

差が小さすぎるので「廃止が優れている」ではなく「**差が無い**」が正しい読み。
効果が無いなら、規則を 1 本に減らして「整っている間は順番に並べるだけ」と
言い切れるほうを採る。守っている対象の 66% が既に合体不能だったことも一致する。

**測った（n=25、seed=221700）。害は無い**。master 側の worktree で
`bury_block_penalty` をゼロ化する変種を立て、A=master / B=廃止版 で回した
（branch 側では関数ごと消えているので `_apply_variant` から戻せない。逆向きに測る）:

| 指標 | A → B | Δ |
|---|---|---|
| score | 1978.60 → 2014.88 | +1.8% (t=0.35) |
| steps | 207.3 → 210.0 | +1.3% |
| merges | 184.9 → 188.2 | +1.8% |
| cascades | 18.04 → 18.48 | +2.4% |
| max_type | 8.92 → 8.92 | ±0 |
| early_score | 225.44 → 227.60 | +1.0% |

シード対戦 win 13 / loss 12。有意差は無い（±100 点には n≈100）が、**負に振れた指標が
1 つも無い**。同じ日に測った 3 規則が「4 本すべて全指標が負」だったのと対照的。

これは**改善の証拠ではなく、害が無いことの確認**。規則を 1 本減らす根拠としては足りる。

### 効かなかった案（スクリーニングで落とした）

- **`size_order_pair_weight` を 1.5 → 9.0 に上げる**: 一致率 88.6% で手がほとんど
  変わらず、変わった手の逆転増分も **+0.11**（改善しない）。盤面全体のペア数という
  大域統計なので、1 個落としても baseline に埋もれて動かない。**この線は死んでいる**
- 対して「落とした実そのものが作る逆転」を手ごとに数える局所項は、w=1.0 で
  一致率 80.7%・変わった手が平均 **−4.00 ペア**。局所か大域かの差だった

### 材料の計算（Wスイカまでの距離）

spawn は type0-4 一様なので 1 手 6.2 cherry 単位。スイカ 1 個 = 1024 単位、
W スイカ = 2048 単位。実測（6 局平均）は 192 手で盤上 1217 単位、必要量の 59%。

ただし**面積で見ると足りている**。同じ 1024 単位を持つのに必要な面積は
スイカ 1 個で 39k、メロン 2 個で 57k、オレンジ 64 個で 299k、cherry 1024 個で 651k
（**まとめると 17 分の 1**）。盤は死亡時に実測 135k を持てているので、
2048 単位をスイカ 2 個（79k）で持つのは物理的に入る。
**足りないのは材料でも手数でもなく、まとめ方だけ。**

### 指標: 逆転率は A/B に使えない

「盤面の綺麗さ」を A/B 指標にするのは測って否定した（n=24）。

- 全手平均の逆転率と score の相関は **+0.36**（汚いほど高得点という逆符号）。
  長く生きるほど実が増えて逆転も増えるので、対局長と交絡している
- 固定手数（40/60/80/100 手目）で交絡を外すと r = 0.00〜0.12 で無相関
- 比較: `steps` r=0.95、`cascades` r=0.76

**逆転率は「1 手が盤をどれだけ汚したか」という手ごとの差分としてだけ使う。**
同一局面で候補どうしを比べる決定的な量なので noise が無く、数分で足切りできる。
エピソード単位の集計値として A/B に載せてはいけない。指標は `cascades` のまま。

### 教師データの上位だけを選んで BC を回す案

**やらない。** 2 つとも測定済みの理由がある。

1. 生徒が教師に届いていない（[BC が match 60-70% に届かない](#調査済み-bc-が-match-60-70-に届かない-2026-08-05)）。
   フィルタしていない教師にすら適合できていないので、上位に絞ると的が鋭くなるだけ
2. **`choose_x` は決定的なので、シード間の score のばらつきは 100% ツモ順の運**
   （n=24 で平均 2012・SD 332）。上位を選んで模倣すると「運が良かったときの
   打ち方」を学ぶことになる

やるなら順番が逆で、先に `src/training/encode.py` を候補条件付きにする
（各候補 x の `simulate_drop` 結果を入力に入れ、生徒は並べ替えだけする）。
それが通れば、教師側を 8/16 の広い探索にするのも筋が良い
（→[探索幅 8/16 の再測定](#探索幅-816-の再測定-2026-08-17)。収集コスト 3.68 倍は教師なら一度きり）。

## 着手タイミング

- 動いている盤で x を決めない。先読みより静止待ちを優先（`src/game/settle.py`）
- 瞬間速度だけでなく、静穏中の横ドリフトでも creep を待つ

## 方策 (bootstrap) の設計

**優先順位**: 盤面を崩さないことがスコアより上。最終目標は score の平均ではなく
ダブルスイカで、大きい側はオレンジ 1 個かデコポン 2 個をツモるまで待って発火する
（`src/ladder.py` が検出だけしている形）。減点の重みを決めるときは、事故回避側が
大小順・閉じ込めを上書きしていないか先に確かめる。根拠は
[盤が崩れる仕組み](#調査済み-盤が崩れる仕組みと段階の切り分け-2026-08-18)と
[材料の計算](#材料の計算wスイカまでの距離)。

- `src/policy.py` は RL 前の薄い方策。合成・危険高さ・埋め込み・薄い大小順・転がり／弾かれの事故防止だけ
- 落下・衝突・合成の物理は pymunk（`src/sim/sim_physics.py`。UT は `tests/sim/test_sim_physics.py`）。
  `choose_x` も同じ `simulate_drop` を採点に使う
- 手の採点は `eval = score - penalties`。加点は本家点だけで、危険高さ・事故・埋め込みは減点で表す
- next 先読み: held の eval 上位 `HELD_TOP` 本だけを、刻み `NEXT_CANDIDATE_STEP` の候補で
  再評価（`NEXT_DISCOUNT` 掛け）。物理が重いので held より粗い
- 埋め込みが主減点。同種ペア待ちを、より大きい異種で直上・肩から塞ぐ手を強く引く
- 異種中央狙い（`FOREIGN_AIM`）: 真下の実が異種かつ中心帯（`FOREIGN_AIM_CENTER_FRAC`）なら
  `FOREIGN_AIM_PENALTY`。真下が同種なら OK。`merges` では切らない（異種真上から転がって
  合体する抜け道を塞ぐ）。谷・肩への異種積みそのものは禁じない（この減点は中心帯だけ）
- 同種過多（`EXCESS_SAME`）: 同種が3個以上たまったら超過1個あたり20の減点
- 谷育成（`_valley_grow_ok`）: 谷の実が held と同種、または谷の実が held のひとつ上で
  held と next が同種なら、その谷への着地に `VALLEY_GROW_BONUS` のボーナス。基準は谷の実で
  壁の型は見ない。それ以外の隙間埋めは通常減点（`GAP_JUNK` は廃止のまま）
- レイアウト: 大実どうしは近接。大側（`sign`）で端に付いた L の外側かつ L 中心より下の
  角ポケットを強く減点（`_big_layout_penalty`）
- ただし隣り合う 2 つの間に型が抜けているなら、そのぶんの隙間は詰めない。詰めると
  抜けている型をツモったとき置き場が無く、外側へ回して並び順を壊すしかなくなる
  （初手 orange の真横に grape を寄せると次の dekopon が外側に落ちて 4-2-3 になった）
- 持たないもの: 押し込み合成、復元押し、連鎖隙間空け、一段上への強制寄り、梯子発火のハードコード
- 具体手順の UT は増やさない。壊れたら事故防止か観測側を見る
- 探索コストはほぼ `simulate_drop` の呼び出し数。`HELD_TOP` / `NEXT_CANDIDATE_STEP` が実行時間を
  決める（旧 8/16 は 1 手 3.8 秒で収集が回らなかった。2/32 で 1.2 秒・score -3.4%）。
  ただし 1 回あたりの単価は 2026-08-17 に 2.44 倍速くなっている
  → [物理の高速化](#物理の高速化-2026-08-17)。速くしたうえで 8/16 を測り直したが
  **採用しなかった** → [探索幅 8/16 の再測定](#探索幅-816-の再測定-2026-08-17)
- `CANDIDATE_STEP` は粗くしない。20 にすると危険な山の真上がグリッドに乗って
  `test_avoids_dangerous_tall_stack` が落ちる。速度は先読み側で稼ぐ
- `SLEEP_FRAMES` を削るのは無効。単発の `choose_x` は速くなるが、盤の収まりが変わって
  次手以降が重くなり、エピソード全体ではむしろ遅い（25 で実測）。物理の忠実度
  （`SimEnv` と共有）も落ちる

### 物理の高速化 (2026-08-17)

`simulate_drop` の単価を **9.42ms → 3.86ms（2.44 倍）** にした。play は完全に不変
（下の検証を参照）ので、score では測っていないし測る必要もない。

**どこに消えていたか。** `choose_x` の 99.2% が `simulate_drop`（1 手 72 回）。
その中身を実時間で測ると、C の物理（`cpSpaceStep`）は 13% しかなく、
**57.9% が `_find_merge_pair`** だった。同種ペアを探すだけの読み取り専用の走査で、
サブステップごとに全実の position / velocity を pymunk のプロパティ経由で読む。
10 実の盤で 1 ドロップ 55,104 回呼ばれて、**戻り値は 100% None** だった。

**やったこと。**

| 変更 | 効果 |
|---|---|
| `_MergeScan`: 一番近い同種ペアの隙間と最大速度から「あと何サブステップは接触しえないか」を出し、そのぶん走査を飛ばす | 走査 55,104 → 3,717 回（1/14.8）。9.42 → 4.43ms |
| `_all_quiet` を後ろから見る（落下中の実は末尾なので 1 個目で打ち切れる）、`_QuietGate` のスナップショット平坦化、慣性モーメントの型ごとキャッシュ | 4.43 → 3.86ms。序盤の疎な盤では 3% しか効かない |

隙間が閉じる時間の下限は、速度が重力でしか増えない（`elasticity=0`）ことから
`g*T² + 2vT = gap` の正の解で出る。押し出し補正（`space.collision_bias`）は
`body.velocity` に出ないので、速度側に 60px/s の下駄（`SCAN_SPEED_MARGIN`）と
16 サブステップの上限（`MAX_SCAN_SKIP`）を掛けてある。

**検証のしかた（この種の変更では score を測らない）。** 各手のあとの全実の x / y を
`repr()`（float そのまま）で、`choose_x` が選んだ x・score・合成回数と一緒に記録し、
master の worktree の出力と**バイト比較**する。3 seed × 70 手と 2 seed × 170 手
（実 15 個・score 1633/1669）が全て一致。スキップ幅が 1 サブステップでも長すぎれば
合体がズレて以降の軌道が全部変わるので、緩い検査ではなく敏感な検査になっている。

**残りの伸びしろは小さい。** 内訳は物理 ~62% / 静穏ゲート 18.5% / 走査 10.8% /
盤の組み立て 5.9%。物理を削るにはサブステップ数かフレーム数を減らすしかなく、
それは play が変わる（`SLEEP_FRAMES` の項も参照）。

### 探索幅 8/16 の再測定 (2026-08-17)

物理が 2.44 倍速くなったので、コストを理由に諦めていた `HELD_TOP=8` /
`NEXT_CANDIDATE_STEP=16` を測り直した。**効果は正だが有意にならず、コストに
見合わないと判断して採用しなかった**（`_apply_variant` の変種は戻してある）。

**先に安いスクリーニングをした。** 幅を広げても手が変わらないなら score 差は
出ようがないので、同一局面で A と B が選ぶ x の一致率を先に見る（決定的なので
noise なし・数分）。210 局面で **一致 75.2%**（食い違い時の x の差は中央 24px・
最大 248px）。4 手に 1 手は別の手なので、測る価値ありと判断して A/B に進んだ。
**この順番は次からも踏むこと。** 数時間を賭ける前に数分で足切りできる。

**n=100 の結果**（`--max-steps 400`、打ち切り 0 本、2.4 時間）:

- score 2105.7 → 2185.3（**+79.6 / +3.8%**、t=1.68、95% CI **[-14.3, +173.5]**）。**有意でない**
- ただし **9 指標すべてが B 側に振れた**。`cascades` +6.6% だけは有意
  （t=2.36、CI [+0.2, +2.2]）。シード対戦 win 59 / loss 41 / tie 0（符号検定 p≒0.07）
- 点推定 +3.8% は、旧記録の「2/32 で -3.4%」と**独立な測定でほぼ一致**した。
  単独では有意でない 2 つの測定が同じ大きさを指している
- 有意にするには n≈136。あと 36 本（約 50 分）で届く距離ではあった
- **分位点どうしの直接比較では、下を持ち上げて上を削っている**:
  最小 1148→1499、10% 1531→1816、中央 2076→2179、90% 2722→2620、最大 3489→3215。
  先読みを広げると事故は減るが大伸びも起きにくい。ただし **type10 到達は 12→20 本**と
  増えており、最大値の低下は突出した 1 局が出なかったことの影響が大きい

**採用しなかった理由**: 1 手 233ms → 856ms（**3.68 倍**）に対して +3.8%。
教師収集（`train_sim.py`）が丸ごと 3.68 倍高くつくのが重い。効果自体は正なので、
**将来もっと安い先読みが書けたら再検討する価値はある**（否定された案ではない）。

### 現行ペナルティルール一覧

`eval = score（合成点） - penalties（事故・悪手の減点）`。
- 手ごとの減点は `_evaluate_drop`、
- 盤面全体の減点は `_board_penalties`（`src/policy.py`）でそれぞれ加算される。

**手ごとの減点（`_evaluate_drop`）**

| ルール | 関数 | 内容 | 重み |
|---|---|---|---|
| 異種ガチ真上 | `_foreign_aim_penalty` | 落下列の真下（中心ズレ±20%以内）が異種のとき | 固定 100.0 |
| 床埋まり後の小側逃げ | `_packed_small_side_penalty` | 床が詰まった後、大きめのツモ（デコポン・オレンジ）を小側に逃がすとき（小側に物理的に置けない場合のみ発火）。床の埋まり判定はそのツモの直径基準（→[床の埋まり判定](#決着-床の埋まり判定はツモ基準-2026-08-19)） | 固定 8.0 |
| 谷育成ボーナス | `_valley_grow_ok` | 谷の実が held と同種／谷の実が held のひとつ上で held と next が同種、その谷への着地 | **−3.0**（`VALLEY_GROW_BONUS`。この表で唯一のボーナス） |

下の 3 つは **held 自身が合体しなかったときだけ**掛かる（`held_merged`。合体の有無
`merges` ではない。盤の別の場所で無関係な合体が起きただけで免除されないように）。

**盤面全体の減点（`_board_penalties`、落下後の盤に対して毎回）**

| ルール | 関数 | 内容 | 重み |
|---|---|---|---|
| 危険な高さ | インライン | 最上部の頭頂が danger_y(70.9) より上のとき | (danger_y − crown) × 0.5 |
| 埋め込み度 | `_bury_penalty` | 合成候補の実が異種で覆われている度合い（兄弟あり1.0／なし0.35） | bury_weight 20.0 倍 |
| 同種過多 | `_excess_same_penalty` | 同種が3個以上（2個までは合成待ちとして許容） | 超過1個につき 20.0 |
| 大小順逆転 | `_size_order_penalty` | 左右で大小が逆転しているペア（より大きい実の谷にはまっていて、**かつ盤に同種の相方が残っている**実だけ対象外＝`_size_order_exempt`。相方のいない谷の実は数える）。**held が合体した手では免除**（合体の反動で弾かれた無関係の実まで違反として数えないため） | ペア差×1.5 ＋ ideal_x乖離×0.004 |
| 大実レイアウト | `_big_layout_penalty` | ① 最大実が大側壁に付いているのに、その外側・下に小実がある（角ポケット埋まり）② 大実どうしの近接不足（間に抜けている型の直径ぶんは免除） | ①50.0×(1+0.05×型差)+depth×0.15　②(隙間−抜け型の直径)×0.025×サイズ係数 |
| 凸凹（高さの分散） | `_height_variance` | 列ビンごとの頭頂高さのばらつき（危険高さ時は0.15倍に縮小） | 分散×0.08 |

補足:
- 上のルールに ON/OFF トグルは無い（恒久化した規則のトグルは残さない方針。
  A/B の手順は [AGENTS.md](AGENTS.md#方策学習を触るとき)）
- 梯子検出（`src/ladder.py`）は現状ペナルティには未使用（検出のみ）

## 学習まわり

- **score / penalties**: `score` は本家の合成点（1〜65、減点なし）、`penalties` は事故・悪手の減点。
  `eval = score - penalties` は **bootstrap の手選び専用**（`choose_x`）。生徒の良し悪し・保存・
  ログは本家点 `score`（同点なら手数）。**RL の報酬も score**（密な減点は報酬にしない）
- `src/reward.py`: `merge_score(merge_types)` が本家と同じ合成点のみ（cherry→0 …
  watermelon 55、ダブル消去 65）。生存加点・死亡減点なし。エピソード終了は従来どおり
  （負けライン／ダブル消去）
- `src/training/encode.py`: 固定長観測ベクトル
- `src/sim/sim_env.py`: 画面なし落下 sim（`sim_physics.simulate_drop`）。`SimStep` は本家点 `score`
  のみ（累積 eval は持たない）
- 評価: `python scripts/eval_policy.py`（`--policy bootstrap|learned`。`--workers` 既定=論理コア/2）
- A/B: `python scripts/compare_policy.py`。bootstrap の 2 変種を対戦させ、平均だけでなく
  シードごとの勝ち負けとフェーズ別（`early_score` / `early_crown` / `dead_early` / `cascades`）と、
  指標ごとのペア差の t 値・95% CI を出す。**比較したい変更は `_apply_variant` に差す**
  （空だと A と B が同じになり、全シード tie の警告が出る）。1 本でも `max_steps`
  打ち切りがあれば警告。`--seed` は省略でランダム（固定シードの使い回しは誤読のもと）。
  `--out` で生データを JSON 保存
- 代理指標の探索: `python scripts/analyze_ab.py <dump.json>`（→[測定のしかた](#測定のしかた何度も踏んでいる罠)）
- 統計は `src/util/stats.py`（ペア差の t / 95% CI / 必要 n / 相関）。scipy は入れていない
- 学習: `python scripts/train_sim.py`（収集 → オフライン BC。既定 max-steps=300 は打ち切りであり
  負けラインではない）。best は score → 手数 → match
- 教師収集は `ProcessPool` 並列（既定 workers=論理コア/2。9700X なら 8。`--workers 1` で直列）
- `src/training/agent.py`: 離散列 32 ビン / hidden 128 の MLP（旧 20/64 の npz は再学習が必要）
- 実プレイ: `python main.py`（npz があれば既定 learned。`L` で bootstrap 切替、`--policy bootstrap`）

## 予定: RL (REINFORCE)

- いまはまだ早い。BC が浅いと素の REINFORCE は壊しやすい（過去に確認済み）
- 足す条件: `match` が高め（目安 60–70%+）かつ生徒の `score` が bootstrap に近い
- やり方: BC 完了後に短い微調整だけ（例: `--episodes 50 --lr 0.002`）。既定はオフのまま
- それまでは BC（収集量・epoch）を厚くする方が先

### 調査済み: BC が match 60-70% に届かない (2026-08-05)

3500 点を狙って RL に進もうとした際、まず既存チェックポイント
（`artifacts/policy_sim.npz` / `policy_sim_rl.npz`）を計測したところ、
score は bootstrap (~2000〜2150) の半分程度 (~1040〜1060) しか出ず、
RL 着手の条件（match 60–70%+）に遠く届いていなかった。

**見つけたバグ (修正済み)**: `src/training/encode.py` の `MAX_FRUITS` が 16 で、
盤面の実を大きい型から優先して詰め、超過分は容赦なく切り捨てていた。
終盤は 20 個超の盤も珍しくなく (実測 23 個)、真っ先に切り捨てられるのは
cherry/strawberry など低段位の散在実 —
[終盤の低段位散在による即死](#調査済み-終盤の低段位散在による即死)
で特定した、まさに事故の張本人が生徒から見えていなかった。32 に上げて実質
切り捨てなしにした。

**それでも解決しなかったこと**: 修正後に BC を既定設定 (収集100 ep, 80 epoch)
で再学習しても match は 12.9%、score は 1242 止まり（epoch を経ても loss が
ほぼ動かず、明らかな under-fitting）。教師データ (150 ep, n=32562) をキャッシュして
使い回し、以下を同一データで比較したがいずれも match 30% に届かず、score も
900〜1150 で頭打ち:

- lr 0.05〜1.0 × epoch 80〜300 × hidden 128/256 の総当たり（最良: hard label,
  lr=0.5, hidden=256, epoch=300 で match=29.1%）
- 教師の連続 x をそのまま one-hot にする hard label 方式 vs 近傍ビンにも
  重みを配る soft label 方式 (`teacher_action_target` / `bc_update_dist`、
  既存コードにあったが学習パイプライン未使用) の比較。soft でも match は
  むしろ hard 最良より低い (15〜21%) 止まり

**分かったこと・仮説**: bootstrap は候補ごとに実際に `simulate_drop` を
回して結果を比べる方策で、`src/training/encode.py` の静的な特徴量 (実の type/x/y/r
の羅列) だけから 1 隠れ層 MLP がその「候補を物理で試してから選ぶ」判断を
真似るのは、学習率・エポック数・soft/hard ラベルのどれを振っても頭打ちに
なった。表現力 (隠れ層の広さ・層数) か特徴量設計 (候補ごとの着地結果を
特徴に含めるなど) 自体を変えない限り、この構成での BC は RL 着手条件に
届かない可能性が高い。RL は未着手のまま。
