# 既知の問題・あとで直すこと

## HELD / NEXT が消えて進行停止（盤面上辺付近）

- **症状**: パイナップルが上からはみ出そうな位置にあると、HELD と NEXT が検知されなくなり自動進行が止まる
- **再現の目安**: 盤面上辺付近に大きい実（パイナップルなど）がある局面
- **見そうな場所**: `src/vision/held.py`（帯下端のはみ出し実）
- **メモ日**: 2026-07-30
- **状態**: 対応済み — 帯マスクから下端に接する／下寄りの塊を除く (`_without_overhang`)。`tests/test_held.py` で固定

## NEXT / HELD を考慮した配置になっていない

- **症状**: 例えばグレープを大きくしたい局面で、NEXT と HELD がイチゴなのにグレープの上に置いてくれない
- **想定原因**: ポリシー／評価が「今持っている実」中心で、NEXT・HELD を踏まえた連鎖・育成を十分に見ていない
- **見そうな場所**: `src/policy.py`, 配置スコア／候補評価まわり
- **メモ日**: 2026-07-30
- **状態**: 対応済み — held/next 同種のとき育成対象の並ぶ側／大側寄せを優先 (異種の中央真上は不可)。`tests/test_policy.py` の局面 UT で固定

## 転がりの未予測で大小順が崩れる

- **症状**: 序盤にブドウの左上へイチゴを置き、左へ転がってイチゴが左・ブドウが右になり大小順が崩れる。右端イチゴの左隣にブドウを置こうとして接触→左端まで弾かれ、デコポンの置き場がなく崩壊する、など
- **想定原因**: 着地を落下列に固定し、側面ヒット後の転がり／床の惰性滑り／合成の寄りを見ない。小さい実の隣より absolute ideal を優先して隙間・接触事故を招く
- **見そうな場所**: `src/policy.py` (`_settle_x`, `_coast_on_floor`, `_wrong_side_roll_penalty`, `_smaller_neighbor_x`)
- **メモ日**: 2026-07-30
- **状態**: 対応済み — 側面転がり＋床滑り、大側落下・弾かれ距離の減点、小さい実のすぐ隣を優先。`tests/test_policy.py` で固定

## 動いている盤面で着手していた

- **症状**: 転がり／連鎖の途中でも held が見えると capture → 方策判断してしまう。動いてる座標で AIM して外す
- **想定原因**: `wait_playable` が settle タイムアウト後でも `ready` なら返していた。Tracker 平滑化で遅い転がりが静止扱いになりやすい。静止前の観測で `choose_x` していた
- **見そうな場所**: `src/settle.py`, `src/env.py` (`step`), `src/observe.py` (`raw_fruits`)
- **メモ日**: 2026-07-31
- **状態**: 対応済み — 静止は raw 座標で判定。`step` 内で静止後の同じ観測から列決め→aim。`tests/test_env.py` で固定

## 端置きで視点が振り過ぎる

- **症状**: 右端に置くとき、(1) 壁際の微調整で視点だけ進む (2) 毎回中央へ戻す往復、で視点が無駄に振れる。左端も同様
- **見そうな場所**: `src/control.py` (`aim`, `recenter`), `src/env.py`
- **メモ日**: 2026-07-31
- **状態**: 対応済み — 端バンドで早期打ち切り＋held 非移動 stall。`recenter` は端からの軽い引き戻しのみ。`tests/test_control.py` で固定

## オレンジとブドウの谷にチェリーを詰める

- **症状**: 序盤で held=チェリーをオレンジとブドウの間 (密着の谷／肩) に落とす。発火点を埋め、大小順も崩しやすい
- **想定原因**: `_gap_junk_penalty` が床着地だけ見ていて、肩の谷を見逃していた
- **見そうな場所**: `src/policy.py` (`_gap_junk_penalty`)
- **メモ日**: 2026-07-31
- **状態**: 対応済み — 床でも谷でも減点。`tests/test_policy.py` で固定

## 端チェリーのあとイチゴが対岸へ弾かれる

- **症状**: 1手目チェリーを右端、2手目イチゴを「真上」(落下列制限で少し左) に落として肩に当たり、左端まで滑って右端チェリー／左端イチゴになる
- **想定原因**: 方策は左隣を選ぶが、aim の端早期打ち切りが `EDGE_BAND` 全体に効いていた。端寄りの内側狙いでも held が壁際のまま「十分」扱いになり、肩落ちする
- **見そうな場所**: `src/control.py` (`_edge_close_enough`), `src/policy.py` (隣 vs 真上)
- **メモ日**: 2026-07-31
- **状態**: 対応済み — 早期打ち切りは壁そのもの付近だけ。`tests/test_control.py` / `tests/test_policy.py` で固定

## 押し込み狙いが外れる

- **症状**: 同種をくっつける／端へ押す狙いに見えるが、内側に落ちて当てられない
- **想定原因**: 接触ぎりぎりの列を狙うと、aim 誤差で内側に入りやすい
- **見そうな場所**: `src/policy.py` (`PUSH_OUTSET`)
- **メモ日**: 2026-07-31
- **状態**: 対応済み — 押し込み／復元の狙い列を外側オフセット。撃たないゲートは `aim_miss` 多発で弱くなったので撤回

## 後回し

- **学習 (RL)**: 薄い bootstrap 方策で数分手が進むなら入ってよい。局面ごとの置き正解 UT は捨てた（具体ヒューリスティックを増やすと長期癖になる）
- **`10.png` ビジョン**: 同系色マスク融合＋枠外イチゴ。切り出し再設計が必要で、ポリシー／held よりコスパが悪い

## 方策 (bootstrap)

- `src/policy.py` は RL 前の薄い方策。合成・危険高さ・埋め込み・薄い大小順・転がり／弾かれ／隙間ゴミの事故防止だけ
- 持たないもの: 押し込み合成、復元押し、育成優先、連鎖隙間空け、一段上への強制寄り
- 具体手順の UT は増やさない。壊れたら事故防止か観測側を見る

## 学習まわり

- `src/reward.py`: 生存・合成・最大段階・スイカ／ダブルスイカ・死亡
- `src/encode.py`: 固定長観測ベクトル
- `src/sim_env.py`: 画面なし落下 sim (`policy.simulate_drop`)
- 評価: `python scripts/eval_bootstrap.py`
- 学習: `python scripts/train_sim.py` (線形 softmax + REINFORCE、numpy のみ)
- `src/agent.py`: 離散列 20 ビンの線形方策
