# 既知の問題・あとで直すこと

## 目次

- [いまの進め方: seed 642746 の定点観測](#いまの進め方-seed-642746-の定点観測-2026-08-19)
- [未着手の課題](#未着手の課題)
- [測定のしかた](#測定のしかた何度も踏んでいる罠) ← 数字を出す前に読む
- [決着: 同点帯は本当に無差別](#決着-同点帯は本当に無差別-2026-08-19) ← 重み調整の行き止まり
- [進行中: 床埋め後の大ツモ・梯子](#進行中-床埋め後の大ツモ梯子)
- [調査済み: 盤が崩れる仕組み](#調査済み-盤が崩れる仕組み-2026-08-18)
- [試して戻した・廃止した規則](#試して戻した廃止した規則)
- [調査済み: 終盤の低段位散在による即死](#調査済み-終盤の低段位散在による即死)
- [材料の計算（Wスイカまでの距離）](#材料の計算wスイカまでの距離)
- [着手タイミング](#着手タイミング)
- [方策 (bootstrap) の設計](#方策-bootstrap-の設計)
- [学習まわり](#学習まわり)
- [予定: RL (REINFORCE)](#予定-rl-reinforce)

## いまの進め方: seed 642746 の定点観測 (2026-08-19)

**seed 642746 の 1 局だけを見て、明らかな悪手を 1 つずつ潰す。目標は角スイカ
（その先はダブルスイカ）。**

この進め方を採る理由は
[決着: 同点帯は本当に無差別](#決着-同点帯は本当に無差別-2026-08-19)にある。
既存の重みには梃子が無く、A/B で拾える差はもう残っていない。一方で局面単位の
欠陥は決定的な量なので、score のノイズを通さずに見える。

追い方の手順は
[AGENTS.md](AGENTS.md#明らかな悪手が残っているうちは-ab-を回さない)。
ここには見つけた症状と診断だけを残す。

**手番は `view_sim` の footer の `move`（1 始まり）で名指しする。** 方策を変えると
その手より前で分岐するので、同じ手番が同じ局面を指すのは変更前だけ。直ったかを
確かめるときは通しではなく、変更前の手順で当該局面まで再生してから比べる。

### 現状: 落下の校正後の 1 局 (2026-08-20)

`GRAVITY` の校正後、seed 642746 は **209 手で死ぬ**（校正前は 269 手）。
以下はこの 1 局の決定的な量で、score ではない。

- **死因は詰みで、自殺ではない。** 致死候補を 1 本でも含む局面は 10 件あるが、
  **生存手があるのに致死手を選んだ手は 0 件**。最後の 209 手目は全 37 候補が致死
- **詰みは 1 手で来る。** 生存候補は 207 手 51/56 → 208 手 50/61 → 209 手 **0/37**。
  校正前は 10 手かけて削れていったが、いまは直前まで余裕があって突然落ちる

### やらない: 致死フィルタを 2 手先へ深くする (2026-08-20)

生存候補のうち「next をどこへ落としても死ぬ」ものを eval の前に捨てる案。上の 1 局で
測ると、**危険局面 10 件のうち 2 手目の詰みで候補が分かれるのは 0 件**。
208 手目は**生存候補 50 本が全部 2 手目詰み**で、選ぶ余地そのものが無い。

盤は「生存候補 50 本」から「50 本全部が行き止まり」へ 1 手で落ちるので、
**分岐点は 3 手以上前にあって 2 手先読みでは原理的に見えない**。
コストは候補ごとに next の全 x を回す形（探索 30 倍超）なので、危険局面に絞っても割に合わない。

*校正前の物理でも同じ結論だった（危険局面 20 件中 2 件しか分かれず、
どちらも選んだ手は詰み側ではない）。*

### 居座る実 (fossil) の測定 (2026-08-20)

`scripts/fossils.py`。実 1 個ずつに id を振り、合体して消えるまでの手数を数える。
seed 642746（209 手）で **253 個生まれ、age は中央 3 手**。ところが
**12 個 (4.7%) は 50 手以上残る**。頭上の隙間で分類すると（延べ手数ベース）:

| age | touch | near | far | none | 相方あり |
|---|---|---|---|---|---|
| 0-5 | 2.0% | 0.0% | 0.0% | **98.0%** | 58.2% |
| 5-20 | 13.6% | 3.9% | 1.8% | 80.7% | 58.1% |
| 20-50 | 11.9% | 20.1% | 6.6% | 61.3% | 58.8% |
| **50+** | **46.6%** | 16.9% | 2.7% | 33.8% | **66.1%** |

- **短命な実は 98% が頭上に何も無い。** 屋根の有無は居座りとはっきり相関する
- **居座り組は 66.1% の手で相方が盤にいる。** 合体待ちではなく取り逃し。
  ここが「合体しても供給に追いつかない」（→[終盤の低段位散在](#調査済み-終盤の低段位散在による即死)）の
  供給側の穴で、**まだ手当てされていない**
- **数字は診断であって、重みを決める根拠にしてはいけない**
  （→[測定のしかた](#測定のしかた何度も踏んでいる罠)）

### 新しい項を入れたときの実測: 肩乗り (2026-08-20)

**症状**: パイン＋梨が育った盤で、その山の上にさくらんぼ・いちごを置く。
一度載った小実は相方に会えないまま 20 手以上居座る（97〜101 手目でパインの上に、
103〜122 手目で桃の上に、同じ実がずっと乗っている）。

**原因は規則の穴で、重みではない。** 「大実の上の小実」を見る項が 1 つも無かった:

- `_bury_penalty` は `over.type > under.type` の側しか数えない。裏返しの
  「小実が大実に載る」形は素通り
- `_size_order_penalty` は縦に重なったペアを `abs(a.x - b.x) < min(r) * 0.5` で
  同じ列として除外する
- 残った横のペアも `_size_order_exempt` が消す。**相方は盤のどこかに 1 個あればよい**
  ので、97 手目のさくらんぼは反対側の端（x=384）にいる相方を根拠に免除される

97 手目（盤 `appl@51 pine@78 oran@145 grap@209 peac@222 pear@290 oran@324 grap@372 cher@384`、
held=cherry）を候補ごとに割ると、パインの山に載せる 4 本だけが全項ほぼ 0 の eval 0.275、
残りは全部 bury 20.0 が乗って −18.8 以下。**同点帯ではなく、単独の項が順序を決めている**
（→[追い方](AGENTS.md#明らかな悪手が残っているうちは-ab-を回さない) の見分け）。
どこへ置いても何かを埋めてしまう盤で、「載せる」だけが無料だった。

**入れたもの**: `_perch_penalty`（→[ルール一覧](#現行ペナルティルール一覧)）。
`_bury_penalty` の裏返しで、判定は接触ではなく「大実の footprint の中で、下端が
その大実の中心より上」。97 手目のさくらんぼはパインに触れておらず、りんごとオレンジの
谷に載っている（パイン上端との隙間 46.3）ので、接触基準だと拾えない。

**重みの掃引（150 局面 / 5 seed、局面単位の決定的な量）**:

| | master | w=4.0 | w=8.0 | **w=16.0（採用）** |
|---|---|---|---|---|
| master との一致 | (基準) | 86.7% | 81.3% | **80.0%** |
| 肩乗りを作った手 | 38/150 | 30 | 25 | **21** |
| うち避けられるのに残る | 19 | 11 | 6 | **2** |

- **38 件のうち 19 件は全候補が肩乗りになる**（避けようが無い）ので、そこが床。
  w=16.0 は避けられる 19 件のうち 17 件を直す
- **8.0 → 16.0 で一致率は 81.3% → 80.0% しか落ちないのに、直る件数は 13 → 17 に増える。**
  重みを倍にしても手がほとんど動かないのは、この項が二値フィルタとして効いていて
  水準に感度が無いから（下の x0.5〜x2.0）
- さくらんぼ×パインは 64.0 になり、bury 1 個 (20.0) を超えて foreign_aim (100.0) に
  近い。**合体を蹴ってでも肩乗りを避ける**水準だと承知して採っている
- 手の変化 20.0% は bury の窓を直したとき (21.6%) に並ぶ大きさ

**帯の外へ出るか**（254 局面、`--eps 0.1`、`band_escape.py` に `x0.0` を足して測った）:

| 項 | 手が変わる | 帯の外へ出る |
|---|---|---|
| **perch x0.0** | **22.8%** | **22.8%** |
| foreign_aim x0.0 | 22.8% | 22.8% |
| bury x0.0 | 21.3% | 21.3% |
| size_order x0.0 | 18.1% | 16.5% |
| big_layout x0.0 | 19.3% | 3.5% |
| perch x0.5 / x1.5 / x2.0 | 4.3% / 2.4% / 3.1% | 3.9% / 2.4% / 3.1% |

**変えた手が 1 つ残らず帯の外へ出る。** foreign_aim・bury と同じ「掛かるか否か」の
二値フィルタとして働いていて、重みを振る方（x0.5〜x2.0）はいつもの 3〜4% しか動かない。
[既存の重みには梃子が無い](#既存の重みには梃子が無い)の結論どおり、効いたのは
重みではなく**何を減点と見なすかの定義**だった。

**A/B n=10（seed 956314〜、max_steps=400、A=肩乗りなし / B=あり）**:

| 指標 | A | B | Δ | t |
|---|---|---|---|---|
| score | 2173.10 | 2124.60 | −2.2% | −0.26 |
| steps | 223.2 | 215.5 | −3.4% | −0.49 |
| merges | 202.6 | 196.0 | −3.3% | −0.43 |
| cascades | 19.10 | 21.10 | **+10.5%** | 1.36 |
| max_type | 9.20 | 9.30 | +1.1% | 0.43 |

**何も言えていない。** 有意な行は 1 つも無く（score の 95% CI は [−464.8, +367.8]、
ペア差 SD=582.0、±100 点を言うのに n=130）、シード対戦は win 5 / loss 5。

- **−2.2% は 1 シードだけで作られている。** seed=956317 が 3189 → 1780（−1409）で、
  これを外すと平均は +102.7。差の中央値は −3.5 でほぼゼロ
- **max_type 10 に届いたシードは 3 対 3。** 956317・956321 が 10 → 9 に落ちる一方、
  956318・956319 が 9 → 10 に上がる。[縦の大小順を revert したとき](#縦の大小順段階ゲート閉じ込め減点2026-08-18revert-済み)は
  悪化シードが軒並み max_type 10 → 9 だったが、その形にはなっていない
- cascades だけ +10.5%（t=1.36）で、唯一それらしく振れている指標

**読み方**: n=10 は大崩れの検出にしかならず、それは出なかった。改善の証拠でも無い。
規則を残す根拠は依然として上の局面単位の量と帯の外へ出た割合。

## 未着手の課題

**ビジョン**
- `10.png`: 同系色マスク融合＋枠外イチゴ。切り出し再設計が必要で、方策／held よりコスパが悪い

**物理シミュレーション**
- フルーツ同士の摩擦が低そう: 本家よりやたらとフルーツが滑り込む。

**学習パイプライン**
- 学習エピソード長: `train_sim.py` の既定 `max_steps=300` は、校正後の実測
  （中央 234 / p95 303 / 最大 320 手、→[測定のしかた](#測定のしかた何度も踏んでいる罠)）で
  **5〜10% が打ち切り**。**学習では報告バイアスでは済まず、REINFORCE の
  リターンそのものが欠けた値になる**ので、他のスクリプトと揃えて 400 にする。
  コストはほぼ増えない（300 を超える対局は 1 割以下で、その最長が 320 なので、
  上限を外して伸びるのは平均 2 手ぶんまで）

## 測定のしかた（何度も踏んでいる罠）

**score のノイズが非常に大きい。** これが方策改善の一番の壁で、下の試行はすべて
ここに埋もれた。数字を出す前にこの節を読むこと。

- 個体差の標準偏差は ~1000〜1200、同一シードのペア比較でも差の標準偏差は 78〜496。
  ±100 点を有意に見るには **n≈100** 要る。数十エピソードでは足りない。
  score より低分散な代理指標を探す方が早い
- **平均の増減だけで判断しない。** `compare_policy.py` が指標ごとにペア差の t 値と
  95% CI を出す（`src/util/stats.py`）。CI が 0 をまたぐ行はその n では何も言えていない。
  有意でないときは「±100 点を語るのに必要な n」も表示する
- **回す前に必要 n を割り算する。** SD は `compare_policy.py` が毎回印字しているので
  材料は常に手元にある。「underpowered だ」と言いながらサンプルを 2 倍にして
  回し直すのは、届かない距離を測る道具を大きくしただけになる
- 長い実行には `--out artifacts/xxx.json` を付けてシードごとの生データを残す。
  集計より先に書き出すので、集計側の不具合で数時間ぶんを失わない
- **代理指標は `cascades`（1 手 3 合成以上の回数）で確定した (2026-08-17)。**
  独立な 3 ダンプすべてで上位に来て、感度比 1.34 / 1.28 / 1.40、r(score) 0.83 で安定。
  n を減らす効果は感度比ぶん（1.4 倍なら必要 n が約半分）で、万能ではない。
  選定は `scripts/analyze_ab.py <dump>`（指標を n_detect で並べる）
- **score で検証していない代理指標を、重み決めの根拠に使ってはいけない。**
  自作の構造指標（閉じ込め数・逆転数）で足切りして重みを決めた 3 規則は、
  スクリーニングで 3 つとも「良い」と出たのに A/B では全指標が負だった
  （→[試して戻した規則](#試して戻した廃止した規則)）。
  同じダンプで一番良く見えた指標を採るのも選定バイアス
- **逆転率はエピソード単位の指標としては使えない (n=24)。** 全手平均の逆転率と
  score の相関は **+0.36**（汚いほど高得点という逆符号）で、対局長と交絡している。
  固定手数で交絡を外すと r = 0.00〜0.12 で無相関（比較: `steps` r=0.95、
  `cascades` r=0.76）。**「1 手が盤をどれだけ汚したか」という手ごとの差分としてだけ使う**
- **打ち切りが出たら数字を割り引く。** 打ち切られるのは伸びた対局なので、
  良い変更ほど過小評価される。**落下の校正後に取り直した自然終了は n=60 で
  平均 239 / 中央 234 / p90 290 / p95 303 / 最大 320 手**（`eval_policy.py
  --max-steps 400` が `打ち切り=0/60`）。`--max-steps` は **320 で 0%、
  300 だと 5〜10%**（p95=303）。既定の 400 は据え置き。200 だと 64%、
  **100 だと 100% が打ち切り**（08-03 の事故はこれ）。`compare_policy.py` は
  1 本でも打ち切られたら警告する
  - 上限を決めるのに要るのは平均ではなく裾なので、`eval_policy.py` は分位点も出す。
    **打ち切り 0 の実行が 1 本あれば、任意の上限に対する打ち切り率がそこから読める**
    （上限ごとに回し直さない）
- **出力で層別して同じ出力を比べない（平均への回帰）。** A のスコアで上位/下位に
  分けて A と B を比べると「上位が悪化・下位が改善」が必ず出る。
  分布を比べたいなら分位点どうしを直接比べる
- **1 手の価値をロールアウトで測ることはできない。** 局面から手を変えて最後まで
  打たせても、盤がそこから完全に分岐するのでツモ運が消えない。同じツモ種で
  ペアにしても差の SD は 580 → 449 にしか落ちない。**eval 最下位の悪手ですら
  選んだ手と区別が付かない**（400 ペアで Δ+31.8・t=1.37・勝率 49.8%）。
  必要 n は 31.8 点差で約 850 ペア、本命の 8.4 点差なら **1 万ペア超**。
  差を見たいなら局面単位ではなく**方策単位**（`compare_policy.py`）にする
- シードは固定しない（`--seed` 省略でランダム）。固定シードの使い回しは、たまたまの崩れを
  「再現した」と誤読しやすい。変更の比較は同一シードでペアにする

測る手順そのもの（A/B の差し方は [AGENTS.md](AGENTS.md#方策学習を触るとき)、
作業ツリーを汚さない比較は [AGENTS.md の git](AGENTS.md#git)）はあちらに置く。
ここは何を信じてよいかだけを書く。

## 決着: 同点帯は本当に無差別 (2026-08-19)

**08-19 の一連の測定でいちばん効く結論。** 帯を割る介入を 4 つ試して全部落ちた
あとで、帯そのものを測って決着が付いた。

**同点帯の中の順位をわざとランダムに入れ替える変種を n=133 で測った**
（held eval に [0, 0.1) の一様乱数を足してから並べ替える。帯の外の順位は変わらない。
乱数は盤面から決定的に作るので再現性は保つ）:

| 指標 | A | B (帯をランダム) | Δ | t |
|---|---|---|---|---|
| score | 2111.60 | 2105.01 | −0.3% | −0.13 |
| steps | 218.4 | 217.7 | −0.3% | −0.18 |
| merges | 196.0 | 195.3 | −0.3% | −0.15 |
| cascades | 18.69 | 18.74 | +0.2% | +0.08 |
| max_type | 9.02 | 9.03 | +0.2% | +0.20 |

win/loss 68/65、ペア差 −6.6（差の SD 582）。**帯の中をでたらめに選んでも何も
失われない。** 当時の tie-break（凸凹の小数第 3 位）は仕事をしておらず、のちに
[廃止した](#廃止-凸凹高さの分散-2026-08-21)。いま帯の順位を決めているのは
`center_tiebreak` だが、**この結果があるので順位付けに意味を求めないこと。**

**「プラトーは欠陥」という枠組みが間違いだった。** 帯の候補は本当に同じくらい良い
未来につながっている。方策は「これらの手は等価だ」と正しく認識していたのであって、
選べていなかったのではない。

### 帯の実態

428 局面（60 手目以降、6 seed）で測った。候補は中央 43 本。

- **プラトーは常態。** 43 候補のうち **9 本**が eval 0.1 以内に並ぶ（eps=0.5 で 11 本、
  eps=2.0 で 14 本）
- **帯は 3 割が候補刻みの artifact、7 割は本物。** 帯の中の相異なる着地結果は
  中央 2 通り。31.8% の局面は帯が 1 通りの盤に潰れる（`CANDIDATE_STEP = 12.0` が
  物理の解像度より細かいだけ）。残る **68.2% は本当に別の盤を 2 通り以上含み、
  22.0% は 5 通り以上**
- **帯の中では大きい項がすべて飽和している。** 帯 (eps=0.1) の中で候補間に差が出るのは
  `size_order` の ideal 項だけで、bury / perch / foreign_aim / excess_same /
  size_order のペア項は帯の中では 99.7〜100% の局面で完全に平ら
  （帯の外へ叩き出す仕事はしている）
  （→[合成項をサブ項へ割る](#合成項をサブ項へ割る-2026-08-21)。この節の当初の数字は
  `packed` が居て `perch` が無かった頃のもので、現行の項では取り直してある）
- **幅ゼロは「死んでいる」からではない。** 全候補で絶対値を測ると（60 局面 / 2660 候補）
  bury は 41.9% の候補で非ゼロ・最小 −80.0、foreign_aim は 18.6%・最小 −100.0、
  size_order は 70.3%・最小 −200.1、excess_same は 69.8%・最小 −280.0。
  **これらの項は「悪い候補を帯の外へ叩き出す」仕事をしていて、帯に残った候補は
  既に全部その項で同点になっている。** どれも「個数 × 重み」の離散量なので飽和する

### 帯を割ろうとして落ちた介入

| 介入 | 手が変わる | 帯の外へ出る | 結果 | なぜ効かないか |
|---|---|---|---|---|
| 梯子の段数ボーナス | 4.0% (w=80) | — | 足切り落ち | 91.6% の局面で段数を伸ばせる候補が無い。噛む w では発火が取れなくなる |
| 閉じ込め減点 | — | 1.2〜4.7% | 足切り落ち | 帯の中で閉じ込め数が動く局面が 1.2%（eps=0.1）しかない |
| `drop_ideal` w=0.004 | 51.4% | **6.3%** | n=133 で null | 変えた先が全部帯の中 |
| 凸凹 x4 (0.08→0.32) | 17.5% | **7.0%** | n=133 で −1.3% | 同上。帯は 9→6 に縮むが score は動かない |

- **梯子の段数ボーナス**: 足切り条件を測る前に固定した（①手の変化率 3% 以上
  ②着地後の段数分布が上向き ③段 3 以上で合成を取る割合が落ちない）。①を満たすのは
  w=80 だけで、そこでは③が 3/66 → **0/66** に全滅する。段が発火すると 4→0 に落ちるので、
  **発火手だけが −4w を背負う**。この項は梯子を組むことと発火することを取り引きしていて、
  点があるのは発火の側。局面集合は 6 seed の 60 手目以降 428 局面
  （最初に 1〜60 手目で取った集合は段 3 以上が 4 件しかなく、機構が噛む前の盤しか
  見ていなかった。**梯子は床が詰まってから組み上がる形なので序盤の集合では何も測れない**）
- **閉じ込め減点**: 定義は「より大きい実に左右から挟まれ、同種の相方も残っていない実」
  （`_is_nestled`）。帯を eps=2.0 まで緩めても差が出るのは 4.7%。
  帯を割るのに要る eval 差の中央は 0.004 で、「ほんの少し足せば拾えるが、
  拾える局面がほとんど無い」形。08-18 の A/B で効かなかった理由もこれ
- **`drop_ideal`**（= `|land_x − ideal_x(drop_type, sign)| × 0.004`）: 足切りは通っていた
  （帯の中で中央 24.9 ばらつき、幅 > 1 が 71.7% の局面）。**n=25 では良く見えたが
  n=133 で消えた**（score +10.0% t=1.79 → +2.7% t=1.10 / cascades +20.3% t=2.78 →
  +4.7% t=1.58）。n=133 で CI が 0 をまたがないのは `early_crown` だけで **B に不利**。
  win/loss 64/69、ペア差の中央 −8。勝ち 64 本の平均 +526 に対し負け 69 本の平均 −381 で、
  **「良くする」より「振れ幅を広げる」形**。
  *n=25 で有意だった cascades が n=133 で消えたのは記録に値する。代理指標は必要 n を
  1.4 倍ぶん減らすだけで、n=25 を正当化しない。*
  *負けた 1 局 (seed=982108) を再生すると大実の塊が散って見えたが 2 手目で分岐しており、
  同一 428 局面で対にすると塊の広がりに差は無い（Δ +0.05、t=0.66）。
  1 局のトレースから構造の因果を読むと間違える例。*
- **凸凹 x4**: 帯の中で routine に動く唯一の項の重みを上げるという最小の介入。
  倍率と手の変化率は x2 8.4% / x4 17.5% / x8 23.4% / x16 32.2%、帯の候補数は
  9 → 8 / 6 / 4 / 3 と縮む。n=133 で score −1.3% (t=−0.49)、CI [−135.0, +81.7]。
  **既存の 0.08 は動かす理由が無い**

**帯が広いのは症状であって病気ではない。** 任意の連続量で tie-break を決定的に
しても、別の任意な選び方をするだけになる。足切り（帯の中でその量がばらつくか）は
**必要条件であって十分条件ではない**。traction が無ければ確実に効かない
（梯子・閉じ込め）が、traction があっても効くとは限らない（ideal_x・凸凹 x4）。

### 足切りは「帯の外へ出るか」で測る

帯の中が無差別である以上、**「手が何割変わるか」は足切りにならない**。見るべきは
**選ぶ手が元の帯の外へ出た割合**。`python scripts/band_escape.py`
（物理 1 パス＋解析的な掃引、数分）。上の表の `drop_ideal` 50.2% → 6.3%、
凸凹 x4 17.5% → 7.0% がその予測力の検証にあたる。

**新しい項の是非も同じ表で読める。** 項を入れた状態で `x0.0`（その項を切る）を見れば、
既存の重みと同じ土俵に並ぶ。`_perch_penalty` を入れたときの
[実測](#新しい項を入れたときの実測-肩乗り-2026-08-20)がその例。

### 既存の重みには梃子が無い

帯に入る候補を決めているのは大きい項なので、そちらの重みを掃引した。
下は**落下の校正後**に取り直したもの（459 局面・6 seed、`--eps 0.1`、帯の候補数は中央 6）。
x0.0 はその項を切ったとき。

| 項 | x0.0 手が変わる | x0.0 帯の外 | x0.5 | x1.5 | x2.0 |
|---|---|---|---|---|---|
| perch | 27.7% | **27.7%** | 4.8% | 4.1% | 6.1% |
| bury | 17.2% | **17.2%** | 4.4% | 3.3% | 5.7% |
| foreign_aim | 16.1% | **16.1%** | 0.2% | 0.0% | 0.0% |
| size_order | 15.9% | 14.8% | 4.1% | 2.4% | 3.9% |
| variance | 58.2% | 12.9% | 0.4% | 0.4% | 1.1% |
| big_layout | 18.5% | 5.7% | 0.7% | 0.4% | 0.9% |
| excess_same | 3.3% | 3.3% | 1.1% | 0.2% | 0.9% |

- **重みを振る側に梃子が無いのは変わらない**（最大 6.1%）。50% の手を変えた
  `drop_ideal` すら n=133 で null だったので、この幅を測っても何も出ない
- **効いている 3 本（perch・bury・foreign_aim）はどれも「手が変わる ＝ 帯の外へ出る」。**
  掛かるか否かの二値フィルタとして働いていることが、項を切る側からも出る。
  `foreign_aim` は 0.5 倍でも 2 倍でも 0.2% 以下で、重み 100.0 が大きすぎて
  掛かる候補は初めから勝負圏外
- **`variance` は 58.2% 動かして 12.9%、`big_layout` は 18.5% 動かして 5.7%。**
  連続量は帯の中で候補を入れ替えるだけ（`variance` はこの表を根拠のひとつに
  [廃止した](#廃止-凸凹高さの分散-2026-08-21)。表は当時の実測のまま残してある）
- **`excess_same` は切っても 3.3%。** 同種 3 個以上に 20.0 という重い減点なのに、
  選ぶ手をほとんど決めていない。大半の候補で同じ値を取って飽和しているとみられる
  （[帯の実態](#帯の実態)の「離散量なので飽和する」と同じ形）
- **この表は重力を半分にする校正 (GRAVITY 2800 → 1400) を跨いでも順位も構造も変わらない。**
  校正前の測定でも perch 28.3 / foreign_aim 21.3 / bury 20.6 / big_layout 2.1 /
  excess_same 1.5% で、**梃子の有無は物理ではなく方策の形で決まっている**

**結論: bootstrap は重み調整では抜けられないところにいる。**
帯の中は無差別、帯に入る候補を決める重みには梃子が無い、新しいカウント項は帯の中で
動かない、新しい連続項は帯の中で動くだけ、探索を深くする道は
[08-17 に測って見送り済み](#実行コスト-物理の高速化と探索幅-2026-08-17)。
伸ばすなら**帯に入る候補を決める側の定義**（重みではなく、何を減点と見なすか）か、
現在の特徴量では見えない差を見られる評価器（学習した価値関数）。
これは [方策 (bootstrap) の設計](#方策-bootstrap-の設計) が最初から
「RL 前の薄い方策」と位置づけてきたものと一致する。

### 合成項をサブ項へ割る (2026-08-21)

上の表の 7 項のうち `size_order` と `big_layout` は**性質の違う 2 規則の和**で、
粗いまま測ると片方の仕事がもう片方のノイズに埋もれる。割って測り直した
（459 局面・6 seed・候補 中央 43。`band_escape.py` と同じ局面集合の作り方で、
サブ項の合計が本物の eval と一致することは全局面で突き合わせてある）。

**候補間で散らばらない項は、重みをいくら振っても手を選べない**（argmax で消えるため）。

| 項 | 候補間レンジ(中央) | 全候補が同値 | 非ゼロ候補 | \|値\|平均 |
|---|---|---|---|---|
| foreign_aim | 100.00 | 0.4% | 18.3% | 18.30 |
| perch | 48.00 | 9.6% | 80.8% | 94.01 |
| size_order ペア | 48.00 | 1.1% | 73.8% | 34.93 |
| bury | 20.00 | 28.8% | 88.9% | 53.86 |
| size_order ideal | 0.36 | 0.9% | 75.4% | 0.30 |
| excess_same | **0.00** | **60.6%** | 69.2% | 41.10 |
| big_layout 角ポケット | 0.00 | 77.8% | 19.8% | 24.09 |
| big_layout 近接不足 | **0.00** | **81.0%** | 16.0% | **0.14** |

- **`excess_same` は「稀にしか掛からないが掛かると強い」。** 候補間レンジの中央は 0.00 で
  6 割の局面では 43 候補すべてが同値だが、**残る 39.4% では候補間で動く**。動くときの
  重みは超過 1 個 20.0 と重い。中央値だけ見て「定数オフセットだから死んでいる」と
  読むと間違える（一度そう読んで A/B で否定された。下の[実測](#測定-excess_same-は残した-2026-08-21)）。
  上の表で帯の外へ出るのが 3.3% なのは「無力」ではなく「稀」の意味
- **`big_layout` の近接不足項は |値|平均 0.14 点。** 非ゼロは候補の 16% だけで、
  「抜けている型の直径ぶんは免除」がこの項の出力をほぼ食い尽くしている。片割れの
  角ポケットは非ゼロ 19.8% と稀だが平均 24 点の二値フィルタ。粗い表の
  「18.5% 動かして 5.7%」は、**この 2 つが分離した姿**だった。
  近接不足はこのあと [A/B に掛けて廃止した](#廃止-大実の近接不足-2026-08-21)
- **効いている項は例外なく「掛かるか否か」の二値。** 連続量は帯の中で候補を
  入れ替えるだけで、外へは出せない

**帯 (eps=0.1) の中に絞ると、盤面減点はどれも差を作れない。**

| 項 | 帯内レンジ(中央) | 帯内で全候補同値 |
|---|---|---|
| size_order ideal | 0.00 | 55.6% |
| big_layout 角ポケット / 近接不足 | 0.00 | 83.5% / 85.0% |
| その他すべて | 0.00 | 99.7〜100% |

**帯の中で差を作れる項は、そこに何を書いたつもりでも事実上のタイブレーカーになる。**
測定当時にその役をしていたのは凸凹で（帯内レンジ 0.01・全局面で非ゼロ）、それを
根拠に [廃止した](#廃止-凸凹高さの分散-2026-08-21)。いまは `center_tiebreak` が
定義から必ず候補ごとに違う値を取るので、役を明示的に負っている。
**新しい連続項を足すときは、この位置に落ちていないかを見ること。**

## 進行中: 床埋め後の大ツモ・梯子

床が埋まったあと、オレンジ／デコポンなど大きいツモを小さい側へ置いて下の実を潰し
崩れる問題。**`packed_small_side_penalty` は 2026-08-19 に廃止した**
（→[廃止: packed_small_side_penalty](#廃止-packed_small_side_penalty2026-08-19)）。
いまこの害を止めているのは `_bury_penalty`。

**梯子**（角桃の内側の隣に梨、その 2 つの**肩**にリンゴとオレンジ、最後のオレンジで
発火して 4→5→6→7 と連鎖）は、この置き分けルールの結果として自然に出てくる形。
`src/ladder.py` が検出だけ書いてあるが**本番の経路からは一度も呼ばれていない**
（呼んでいるのは `tests/test_policy.py` だけ）。手選びに使うときの土台として残してある。

- **発火に誘導は不要**。梯子が組み上がっていれば `choose_x` は x を全振りした最良と
  同点を取る。発火用の候補 x を足しても no-op だった（削除済み）
- **`FOREIGN_AIM` は梯子と無関係**。段は肩に乗るのでこの減点が掛からない
- **`_size_order_penalty` も邪魔していない**。梯子の盤で実測 0.14〜0.49 しかない
- **床が埋まっていないと形が保たない**。梨が楔で押し出されて自壊し、どこに落としても
  1 段ぶん（15 点）しか取れない。床を右端まで埋めると 100 点。床の詰まりはゲート条件
- **ボトルネックは組み立て**。4 段揃った盤は 720 中 12 回しか出ない。段を「落として置く手」
  として条件を書くのは筋が悪い（ツモれるのは orange まで。梨・リンゴは合成でしか作れない）。
  段数をそのまま報酬にする形も測って見送った
  （→[帯を割ろうとして落ちた介入](#帯を割ろうとして落ちた介入)）
- 小側の room 判定は幾何の隙間幅だけでなく、実際に `simulate_drop` で確認する
  （`_small_side_room_ok`）。屋根で塞がれた隙間を room ありと誤判定するバグを直した

## 調査済み: 盤が崩れる仕組み (2026-08-18)

6 局を手ごとにトレースして数えた（局面単位の決定的な量なので score のノイズには
載っていない）。「盤面を崩さないことがスコアより重要」という方針が、減点の
重み付けでは逆になっていた。

**症状**

- **横の大小順の逆転率は序盤 10% → 終盤 40〜50%**。50% は完全に無秩序
- **縦は最初から無法地帯**。縦に重なった 23079 組のうち 47% が「上のほうが大きい」。
  `_size_order_penalty` は `a.x <= b.x` しか見ておらず、縦の規則が 1 つも無かった
- cherry / strawberry が主犯（1 手あたり +1.25 / +1.57 ペア）。orange だけが −0.93 で回収
- **綺麗な手は候補に入っている。評価が蹴っている**: cherry は汚さない手が候補にある
  局面が 97% あるのに、実際に選ぶのは 64%。1 手あたり 2.85 ペアの取りこぼし

**何が大小順に勝っていたか**（綺麗な手を蹴った局面で、選んだ手 − 綺麗な手）

| 項 | 差 | 決め手になった率 |
|---|---|---|
| `bury_block` | **−7.43** | 35% |
| `FOREIGN_AIM` | −6.12 | 6% |
| 谷育成 | −1.04 | 35% |
| `sizeord` | −0.03 | 16% |

換算すると逆転 1 ペア ≒ eval 4.91 に対し、`bury_block` は型差 1 で 14.0、
`FOREIGN_AIM` は 100.0。**`sizeord` は汚い手と綺麗な手を実質区別していない。**

- `bury_block` は小側候補の 51〜72% で発火し、**守っているペアの 66% は既に
  大きい実が間に挟まって合体不能**。83% は接触距離の 3 倍以上離れている
- 谷育成は発火 1642 件の **100% が大側への着地**。97% は「谷に同種の実があるが
  この手では合体しない」＝その実の隣に積む手だった
- `_size_order_exempt` の免除は従犯。免除を全部外しても 1 ペアあたり 4.91 → 6.20

**副産物として拾えた事実**（規則を戻しても残る）:

- 逆転率はペアの左右だけで数えると足りない。**谷にいる実は左右どちらの壁に対しても
  外している**と数えるべき（`_valley_flanks` を再利用）。この誤りは
  `test_grows_valley_fruit_when_held_and_next_are_one_smaller` が検出していた。
  **テストが正しく、指標のほうが間違っていた**
- 谷の有無だけを盤の状態の判定に使うのは駄目。実 8 個以上では必ず谷があるので常に真になる。
  谷込みの逆転率なら実数別に 98% / 70% / 48% / 16% / 3%（実 0-3 / 4-7 / 8-11 / 12-15 / 16+）
  と勾配が残る

**決着: 床の埋まり判定はツモ基準 (2026-08-19)**

`_floor_packed` の閾値をオレンジ直径の固定値からツモの直径に変えた。
`packed_small_side_penalty` は唯一の呼び元で、`PACKED_BIG_DRAW_MIN_TYPE` により
**デコポンとオレンジでしか走らない**。旧閾値はオレンジ直径 77.1 の固定値だったので
オレンジ側は元から正しく、ずれていたのはデコポン（直径 59.6）だけ。

- 変更前の埋まり判定率（8 seed × 40 手）は実 3〜5 個で **43.8%**、6〜8 個で **89.5%**。
  一列に並んだだけの盤がデコポンのツモに対して埋まり扱いだった
- 旧新を差し替えて同じ局面で `choose_x` を両方回すと（6 seed × 60 手）
  **356/360 手が一致（98.9%）**。減点の発火は 1445 → 1219（**−15.6%**）
- **A/B は回していない。** 1.1% の手の変化は n=25 では score のノイズに埋もれる。
  前提の誤りを直す変更で、点数を動かす主張はしていない

## 試して戻した・廃止した規則

### 測定: excess_same は残した (2026-08-21)

`_excess_same_penalty` を切る A/B、n=100・打ち切り 0。**切ると下がる側に振れた。**

| 指標 | A（あり） | B（切る） | Δ | t | 95% CI |
|---|---|---|---|---|---|
| score | 2329.90 | 2281.26 | −2.1% | −0.84 | [−164.0, +66.7] |
| merges | 214.4 | 209.7 | −2.2% | −0.97 | |
| cascades | 21.83 | 21.41 | −1.9% | −0.71 | |

win/loss 43/55/tie 2、ペア差 −48.6（差の SD=581.2）。**有意ではない**（CI が 0 をまたぐ）が、
凸凹のとき（+1.5%・52/48 のコイン投げ）と違って符号が揃って負。→ **残す**。

**教訓**: [サブ項の表](#合成項をサブ項へ割る-2026-08-21)で候補間レンジの中央が 0.00 だったので
「定数オフセット＝死んでいる」と読んだが、**中央値は「稀」を「無力」と読み違えさせる**。
39.4% の局面では動いていて、動くときの重みは 20.0。同じ形の `big_layout` の角ポケット
（非ゼロ 19.8%・平均 24 点）を「効いている」と評価していたので、判定が一貫していなかった。
**非ゼロ率と、動くときの大きさを一緒に見ること。**

決着させるには n≈549（8 workers で約 6.4 時間）。そこまでの価値は無いと判断した。

### 廃止: 大実の近接不足 (2026-08-21)

`_big_layout_penalty` の後半（大実どうしの隙間を罰する連続量）を切る A/B、
n=100・打ち切り 0。score 2335.57 → 2404.60（+3.0%、t=1.10、CI [−55.5, +193.5]）、
merges +2.9%、cascades +2.0%、win/loss 52/48。**有意ではないが符号は揃って
「切ったほうが良い」側**で、|値|平均 0.14 点という機構とも一致する → 削除。

自分の免除条項に食われていた項だった。大実はふつうサイズ順に並ぶので、隣り合うペアの
間には「抜けている型」がちょうど存在し、その直径ぶんを免除すると隙間が消える。
**非ゼロは候補の 16% だけ、動くときも 0.14 点**（→[サブ項](#合成項をサブ項へ割る-2026-08-21)）。

残った角ポケット側は `_corner_pocket_penalty` に改名した。**1 つの名前に性質の違う
2 規則を入れると、片方の仕事がもう片方のノイズに埋もれて読み違える**——この節と
[excess_same](#測定-excess_same-は残した-2026-08-21) の両方がその実例。
`BIG_CLUSTER_SPAN` も近接不足でしか使っていなかったので道連れ。

**注意**: これより前の NOTES に出てくる `big_layout` の測定値（帯の外へ 5.7% など）は
**2 規則が合わさっていた頃のもの**。当時の記録としてそのまま残してある。

### 廃止: 凸凹（高さの分散）(2026-08-21)

`_height_variance`（列ビンごとの頭頂のばらつき）を切る A/B が n=100・打ち切り 0 で
null。score 2312.42 → 2346.60、t=0.53、CI [−94.5, +162.8]、win/loss 52/48 で、
符号はむしろ切ったほう。**「盤の凸凹を減点する」案は測って落ちている。**
理由は連続量だから——帯の中で候補を入れ替えるだけで外へ出せない
（→[合成項をサブ項へ割る](#合成項をサブ項へ割る-2026-08-21)）。
`DANGER_Y` と `_top_crown` も緩和にしか使っていなかったので道連れ。同点の順位は
`center_tiebreak` が明示的に負う。詳細は `git log -- src/penalties.py`。

### 危険な高さの傾斜をフィルタに置き換えた (2026-08-20)

**きっかけ**: 「危険な高さルールは要らないのでは。W スイカ直前は必ずギリギリになる」。

**ギリギリ具合**: 発動ライン `DANGER_Y` 70.9 に対し負けライン `GAME_OVER_Y` 14.9。
差 56.0 は盤の高さ 500 の 11.2%（床から 85.8% 積んで初めて 0 が付く）で、
りんご 1 個（直径 98.6）が入らない幅しかない。減点は死線ちょうどでも最大 28.0 で、
スイカ合体 55 / W スイカ消し 65 より小さい。**合体を蹴って上がるのを止めることはない。**

**候補ごとの実測**（428 局面・19287 候補、6 seed × 240 手の 60 手目以降）:

| | 割合 |
|---|---|
| `danger` が非ゼロな候補 | 21.4% |
| 致死候補（落下後 crown < 14.9） | 1.1% |
| 致死候補を 1 本以上含む局面 | 30 件 (7.0%) |
| 選ばれた手が `danger` 非ゼロ | 19.9% |
| **選ばれた手が致死** | **5 件 (1.2%)** |

**傾斜が入っていても自殺していた。** その 5 件は生存候補が 30〜45 本あるのに致死手が
勝っており、差は 8.5 / 24.3 / 40.9 / 121.5 / **261.3**。合体点ではなく
`bury`・`excess_same`・`size_order` が差を作っている（致死手側の合体点は 0〜10）。
**上限 28 の傾斜では届かないし、有限の減点をいくつ積んでも 261 は超えられない。**

一方で傾斜を切ると、致死手と生存手が**差 0.00 で並ぶ** 5 件が致死側に倒れた
（計 10 件 2.3%）。つまり傾斜が効いていたのは「タイブレーク」としてだけ。

**入れたもの**: `choose_x` の致死手フィルタ。生存候補が 1 本でもあれば、
致死候補は eval を比べる前に落とす。10 件すべてが直る。全候補が致死の盤
（詰み）では従来どおり最良手を選ぶ。この 2 つの振る舞いは `tests/test_policy.py` に
固定してある（盤は自滅する側が seed=982108 の 172 手目、詰みが seed=221700 の
214 手目。どちらも通し打ちから拾った実局面）。

**消したもの**: `(DANGER_Y − crown) × DANGER_CROWN_WEIGHT` の傾斜と
`DANGER_CROWN_WEIGHT`。`DANGER_Y` は凸凹を緩める閾値として残る。

**通し打ちでの効き**（6 seed × 240 手、同一 seed で master と比較）:

| | master（傾斜のみ） | フィルタ |
|---|---|---|
| 手数 | 1211 | 1299 |
| 生存手があるのに致死手を選んだ | **6 (0.5%)** | 0（フィルタが 35 手 2.7% で阻止） |
| 詰み局面（全候補が致死） | 0 | 4 (0.3%) |
| 死亡 | **6 / 6 局** | 4 / 6 局（2 局は 240 手の上限まで生存） |

**master の 6 死はすべて避けられる死だった。** 詰み局面が 1 件も無い＝死んだ瞬間にも
生存手が残っていたのに致死手を選んでいる。フィルタ側の 4 死はすべて詰み局面で、
自殺死はゼロ。フィルタの発動 2.7% が自殺 0.5% より多いのは、傾斜を消した分だけ
致死手が上位に来やすくなったから（傾斜が抑えていたぶんをフィルタが受けている）。

steps 1211 → 1299、score 11961 → 12862 も出たが、**n=6 の通し打ちなので証拠にしない**
（→[測定のしかた](#測定のしかた何度も踏んでいる罠)）。読むのは上の構造指標だけ。

**score は測っていない。** 傾斜単独の帯の外へ出る割合は 4.7%（eps=0.1）/
3.0%（eps=0.5）で、[足切り](#足切りは帯の外へ出るかで測る)の null 圏
（`drop_ideal` 6.3% → null、凸凹 x4 7.0% → null）。**A/B は未実施**。
根拠は上の局面単位の量だけ。

**注意**: 絶対高さを見る項はこれで無くなった。高さへの抑止は「次の 1 手で死ぬか」
だけになり、2 手先までしか効かない。自分から詰み盤へ歩いていく病理が出ていないかは、
[定点観測](#いまの進め方-seed-642746-の定点観測-2026-08-19)で見ること。

### 縦の大小順・段階ゲート・閉じ込め減点（2026-08-18、revert 済み）

**きっかけ**: view_sim で桃-オレンジ-桃 に崩れた盤が出たという報告。

**因果は 13 手前にあった。** 25 手目、盤は完全な降順（逆転率 0.000・閉じ込め 0）で、
デコポンを落とすとき **いちごを閉じ込める手が、閉じ込めない 13 本を eval 0.07 差で
上回った**（全 33 候補が幅 0.09 の同点プラトー）。28 手目にはもうどこに置いても悪い。
38 手目の 64 点 3 連鎖は取るのが正しく、その結果できた桃が確定した。
**桃-オレンジ-桃 は結果であって原因ではない。**

**作ったもの（すべて revert 済み）と A/B（n=25、同一シード `--seed 526304`）**:

| 変種 | 内容 | score | Δ | cascades |
|---|---|---|---|---|
| 縦の大小順 | 縦に積まれた組で上が大きいとき型差 × 1.5 | 1981.20 | **−8.5%** | −10.8% |
| 段階ゲート | 逆転率がしきい値超の盤でだけ立て直し規則を掛ける | 2035.24 | −6.0% | −3.1% |
| 閉じ込め減点 | 大きい実に左右から挟まれ相方もいない実に 1 個 4.0 | 2077.04 | −4.1% | −5.0% |
| 3 つ全部 | | 2045.60 | −5.5% | −7.9% |

A 側はどれも 2165.44。有意なものは 1 つも無い（±100 点には n≈127 要る）が、
**4 本すべて・全指標が負**。悪化シードは軒並み max_type 10 → 9 で、
ダブルスイカの目標に一番近い指標がはっきり落ちた。

- **段階ゲートは論理的に成立しない。** 谷は「自分より大きい実に左右から挟まれた」形
  なので、**小さい側に大きい実がある時点でそれ自体が並び順違反**。*谷があるなら逆転は
  必ずある*（実測 332 盤すべてで逆転率 > 0）。しきい値 0 なら完全な no-op、
  0 より上なら**壊れた盤で立て直しを禁止する**害だけが出る（谷育成が成立した
  151 局面のうち 28 件を止めていた）
- **縦の大小順は前提が疑わしい。** **合体すると大きい実がその場に生まれる**以上、
  小さい実の上に大きい実が乗るのはこのゲームの正常な姿の可能性がある。
  横は seed=74546 で崩壊まで追えたが、縦は横からの類推だけで規則にした
- **閉じ込め減点は狙いの手は直した**（25 手目の悪手は消えた）が、それでも −4.1%

**外れた見立て**: `packed_small_side_penalty` が犯人 → 違う。241 局面で試して
**一致率 100%**、この規則は手の選択に影響していなかった。局所 ordering 項も
w=4.0 まで振って変化なし（着地が右でも左でも逆転する相手が同じ）。

**注意**: 4 本とも同じ 25 シードなので独立な証拠ではない。ただし seed=74546 の
通し打ちも独立に負を指していた。

**一番の教訓**: 自作の構造指標が 3 回とも逆を指した
（→[測定のしかた](#測定のしかた何度も踏んでいる罠)）。

### 肩の合否を「オレンジ以上」で決める案（2026-08-20、不採用）

`_perch_penalty` が載せてよい実を決める基準を、**最大実との型差**（現行、
`PERCH_MIN_GAP` 5）から**実の型そのもの**（orange 以上なら可）に替える案。

**きっかけ**: seed=956317 の 34 手目。桃 (7) の左肩にデコポン (3) が載り、
40 手目にそのデコポンが梯子の下段のオレンジを食って潰した。型差 4 なので現行は
発火しない（その局面の perch は 0.000 で、size_order 6.078 対 bury 7.0 で肩側が勝つ）。
絶対しきい値にすると 34 手目はデコポンを右 (368,431) へ回し、左の山は
`peac@69 / oran@160 / pear@226` の梯子のまま残る。150 局面の一致率も
80.0% → 79.3% でほぼ同じだった。

**A/B n=10（seed 956314〜、A=現行 / B=案）**:

| 指標 | A | B | Δ | t |
|---|---|---|---|---|
| score | 2124.60 | 1902.90 | −10.4% | −1.14 |
| steps | 215.5 | 202.2 | −6.2% | −0.85 |
| merges | 196.0 | 179.9 | −8.2% | −1.01 |
| cascades | 21.10 | 18.60 | −11.8% | −1.08 |
| **max_type** | **9.30** | **8.70** | **−6.5%** | **−2.71** ← 95% CI [−1.1, −0.1] |

**全指標が負で、max_type だけ有意に負。** n@5% が 9 なので max_type は n=10 で語れる
数少ない指標で、そこが落ちている。悪化シードは 956319 が 10 → 8、956314 が 10 → 9、
956320 が 9 → 8、956318 が 10 → 9。
[縦の大小順を revert したとき](#縦の大小順段階ゲート閉じ込め減点2026-08-18revert-済み)と
同じ形（全指標が負・悪化シードの max_type が落ちる）で、症状はあのときより濃い。

**見立て**: 案は**デコポンをどの大実の肩からも締め出す**が、桃の肩のデコポンは
次の段を作る正しい置き場でもある。34 手目が悪手だったのは肩に載ったからではなく、
**40 手目でそのデコポンが梯子の下段のオレンジを食ったから**で、原因は肩ではなかった。
禁じると中型の実が反対側へ流れて梯子が育たない。加えて案は**メロンの肩のオレンジを
見逃す**方向にも緩む（型差 5 を許す）ので、締める側と緩む側の両方で損をしていた。

**残っている課題**: 34 手目の症状そのものは未解決。追うなら
「肩に載ったこと」ではなく「梯子の下段を食う合体」を見る項になる。

### 廃止: `packed_small_side_penalty`（2026-08-19）

**落下前の幾何だけで発火を決める唯一の減点だった。** 床の隙間（`_floor_packed`）と
小側に置けるか（`_small_side_room_ok`）を落下前に予測し、当たれば固定 8.0 を課す。
2026-08-06 に n=100 で測って **ON で恒久化した**規則（score 2047.0 → 2093.9、**+46.9**、
t=0.85、95% CI [−62.3, +156.1] で**有意でない**。シード対戦 win 52 / loss 38 / tie 10。
有意にするには n=529＝約 14 時間なので、点推定が正・狙いと形が一致・発火 90/100 を
根拠に採用した）。**その判断を覆して削除した。**

**削除の根拠**（125 局面 / 5 seed、局面単位の決定的な量）:

| | |
|---|---|
| 候補ベースの発火 | 189/5146 (3.7%) |
| 局面ベースの発火 | 16/125 (12.8%)。デコポン・オレンジのツモだけ |
| **選んだ手に乗る** | **0/125 (0.0%)** |
| 外すと手が変わる | 6/125 (4.8%)。発火した局面に限れば 6/16 (37.5%) |
| **罰した候補のうち、落下後の盤が「選んだ手」より良いもの** | **75/189 (39.7%)** |

- **この規則は候補を排除する側にしか働いていない**（選んだ手に一度も乗らない）
- **罰した候補の 4 割は、実際に選んだ手より落下後の盤が良い。** つまり
  `board_penalties` と逆を向いている
- `_big_layout_penalty` は packed に**一度も同意しない**。189 候補のうち同じ向き 0 件、
  値が同じ 132 件 (69.8%)、逆向き 57 件 (30.2%、中央 −0.429)。重み 8.0 に対して桁が
  1 つ小さく、対抗できない

**具体例: seed 642746 の 52 手目**（`view_sim` の `move`）。オレンジのツモ、sign=+1。
床の最大隙間 73.2 < オレンジ直径 77.1 なので `_floor_packed` は真、`_small_side_room_ok`
は偽。小側の候補すべてに 8.0 が乗る。盤面減点を項ごとに割ると:

| 項 | x=60（選んだ手） | x=216 | 差 |
|---|---|---|---|
| size_order | 6.213 | 0.055 | **+6.159** |
| big_layout | 0.000 | 0.000 | 0.000 |
| その他 | — | — | ほぼ 0 |
| packed | 0.000 | **8.000** | **−8.000** |
| 合計 | 9.749 | 11.592 | −1.843 |

**`_size_order_penalty` は正しく 6.159 多く課していた。packed の固定 8.0 がそれを
上書きして悪手を勝たせていた。** 結果、左壁とパインの隙間 28.9 に直径 77.1 の
オレンジを押し込み、**パインが角から盤の中央へ 102.6 弾き出される**（壁からの隙間
28.9 → 131.5）。packed を切ると x=216 を選び、逆にパインを壁へ寄せる（28.9 → 3.3）。

**A/B は回していない。** 恒久化の判断を覆すので本来は同じ土俵（n=100）で測るべきだが、
採用時の A/B 自体が有意でなく（CI が 0 をまたぐ）、上の局面単位の証拠は決定的なので
先に削除した。**score での再確認は未実施。**

### `bury_block` は廃止した（2026-08-18）

`bury_block_penalty` は**どちらの構造指標でも効果が検出できなかった**ので削除した。
構造的にも `14.0 × (drop_type - under.type)` は「小さい実の上に大きい実」の型差減点
そのもので、「下の実に相方がいる」条件と 9 倍の重みが付いていただけだった。

166 局面で、消すと手は 10% 変わる。そのうえで盤の汚れ（横＋縦の逆転）は現行 +40 /
廃止 +40 で差し引きちょうど 0、**本来の仕事である合体ペアの保護でも消したほうが
わずかに多く残る**（−2 → ±0）。合体回数は同一。

n=25（seed=221700）で A=master / B=廃止版 を測ると score 1978.60 → 2014.88 (+1.8%,
t=0.35)、steps +1.3%、merges +1.8%、cascades +2.4%、max_type ±0。win 13 / loss 12。
有意差は無いが**負に振れた指標が 1 つも無い**。これは**改善の証拠ではなく、
害が無いことの確認**。規則を 1 本減らす根拠としては足りる。

**注意**: 当時は縦の大小順とゲートが入った状態で測っており、それらは後に revert した
ので、**この廃止だけが未測定のまま残っている**。

### 効かなかった案（スクリーニングで落とした）

- **`size_order_pair_weight` を 1.5 → 9.0**: 一致率 88.6%、変わった手の逆転増分も **+0.11**。
  盤面全体のペア数という大域統計なので 1 個落としても baseline に埋もれる。**この線は死んでいる**
- 対して「落とした実そのものが作る逆転」を手ごとに数える局所項は、w=1.0 で一致率 80.7%・
  変わった手が平均 **−4.00 ペア**。局所か大域かの差だった
- **教師データの上位だけを選んで BC を回す案**: やらない。フィルタしていない教師にすら
  適合できていない（→[BC が match 60-70% に届かない](#調査済み-bc-が-match-60-70-に届かない-2026-08-05)）
  うえ、**`choose_x` は決定的なのでシード間の score のばらつきは 100% ツモ順の運**
  （n=24 で平均 2012・SD 332）。上位を選ぶと「運が良かったときの打ち方」を学ぶことになる。
  やるなら順番が逆で、先に `src/training/encode.py` を候補条件付きにする
- **埋め込みの縦窓を相方の直径まで広げる**（`0.6r` → `2r`）: 帯の外へ出るのは **6.1%** で
  null 圏。局面単位でも一致率 89.9% で、変わった 24 件では狙いの取りこぼしが半減する代わりに
  **普通の埋め込みが増える**（`bury` 自身の帯外率が 20.6% → 14.7% に落ちる＝仕事を
  取っただけ）。**さらに校正後は前提が消えた**: 居座り組が触れる屋根の下にいる割合は
  21% → 46.6% で、現行の窓が既に見えている。*測定は校正前*
- **大実の近接を x の隙間ではなく中心間距離で見る**: 塊項は縦に積まれたペアを「隙間 0」と
  読む（同型の大実ペアの 17% がこの形）。直すと終盤で 5 倍の値になるが、**16.0% の手を
  変えて帯の外へは 1.4%**、重みを振ると 0.0%。`big_layout` 本体と同じ形で、
  **連続量である限り帯の中で入れ替わるだけ**。効かせるなら距離ではなく
  「この手が大実ペアを分断するか」の二値条件にする必要がある。*測定は校正前*

## 調査済み: 終盤の低段位散在による即死

**死因診断**: 全エピソードが `dead` で終了し、`max_steps` 打ち切りには届かない。
1 エピソードを手ごとに再生すると（seed=20260816）、死亡直前の 10 手以上、
cherry 5〜8 個・grape 3 個・dekopon 4 個など低段位の実が合成されないまま散らばって
残っていた。最終手は安全な着地先が無く即死。その手の直前局面で cherry を x=0〜400 の
どこに落としても合成が一切成立しない。つまり「その手」の問題ではなく、もっと前の手で
低段位の実が大きい異種に側面から挟まれ、**物理的に合体不能な位置まで散らばっていた**
ことが根本原因。

**改善試行（いずれも改善を確認できず・コードは revert 済み）**

| 試行 | 内容 | 結果 |
|---|---|---|
| 同種過多の三角数化 | `_excess_same_penalty` を線形から三角数へ | n=24 で +1.9% / n=40 で -3.0% と割れる |
| 側面隔離ペナルティ | 相方のない実をこの手で左右とも塞ぐ手を検出 | n=32 で -9.2% (SE~206 に対し差 195) |
| 3 手先読み | eval 上位 2 本だけ 3 手目を展開。1 手 313→401ms | n=32 で -0.7% |

### 調査済み: eval の加点側は律速ではない (2026-08-17)

**仮説（外れた）**: 加点が本家点そのものなので cherry 合体は 1 点しかなく、一方で
減点は `EXCESS_SAME` 20・`FOREIGN_AIM` 100 のオーダー。だから小さい実を片付ける動機が
構造的に埋もれているのではないか。

**試したもの**: 盤の実の個数そのものを減点にする `FRAGMENT_WEIGHT`。合体 1 回で個数が
1 減るので「型に依らない合体ボーナス」と数学的に同じ形になる。

**A/B を回す前に落ちた。** 一致率スクリーニング（180 局面）で現行と同じ x を選ぶ割合が
**w=3〜15 で 98.9〜98.3%、w=25 でも 94.4%**。

**なぜ効かないのか**（270 手を数え直した）: 合体した手 138 (51%)、**合体できる x が
あったのに見送った手 6 (2%)**、そもそもどこにも合体が無い手 126 (47%)。
**合体可能な局面の 96% で現行方策は既に合体を取っている。** 加点が 1 点でも、合体すれば
実が 1 個減って埋め込み・同種過多・高さの減点がまとめて外れるので、
**減点の解除が加点の代わりをしていた**。

**死因の見方も更新する。** 進行別に測ると（3 局 575 手）「合体できる手」の割合は
序盤 47% → 終盤 57% で**終盤に落ちない**。一方で盤の実数は 4.3 → 17.0 と増え続ける。
終盤の崩壊は「**合体できなくなる**」のではなく「**合体しても供給に追いつかない**」。
1 手で必ず 1 個増えるのに対し単発の合体は 1 個減らすだけで、複数減らせるのは連鎖だけ。
`cascades` が一貫して最も鋭い代理指標なのもこれと整合する。

## 材料の計算（Wスイカまでの距離）

spawn は type0-4 一様なので 1 手 6.2 cherry 単位。スイカ 1 個 = 1024 単位、
W スイカ = 2048 単位。実測（6 局平均）は 192 手で盤上 1217 単位、必要量の 59%。

ただし**面積で見ると足りている**。同じ 1024 単位を持つのに必要な面積はスイカ 1 個で 39k、
メロン 2 個で 57k、オレンジ 64 個で 299k、cherry 1024 個で 651k（**まとめると 17 分の 1**）。
盤は死亡時に実測 135k を持てているので、2048 単位をスイカ 2 個（79k）で持つのは
物理的に入る。**足りないのは材料でも手数でもなく、まとめ方だけ。**

## 着手タイミング

- 動いている盤で x を決めない。先読みより静止待ちを優先（`src/game/settle.py`）
- 瞬間速度だけでなく、静穏中の横ドリフトでも creep を待つ

## 方策 (bootstrap) の設計

**優先順位**: 盤面を崩さないことがスコアより上。最終目標は score の平均ではなく
ダブルスイカで、大きい側はオレンジ 1 個かデコポン 2 個をツモるまで待って発火する
（`src/ladder.py` が検出だけしている形）。減点の重みを決めるときは、事故回避側が
大小順・閉じ込めを上書きしていないか先に確かめる。根拠は
[盤が崩れる仕組み](#調査済み-盤が崩れる仕組み-2026-08-18)と
[材料の計算](#材料の計算wスイカまでの距離)。

- `src/policy.py` は RL 前の薄い方策。合成・危険高さ・埋め込み・薄い大小順・
  転がり／弾かれの事故防止だけ
- 落下・衝突・合成の物理は pymunk（`src/sim/sim_physics.py`。UT は `tests/sim/test_sim_physics.py`）。
  `choose_x` も同じ `simulate_drop` を採点に使う
- 手の採点は `eval = score - penalties`。加点は本家点だけで、危険高さ・事故・埋め込みは減点で表す
- next 先読み: held の eval 上位 `HELD_TOP` 本だけを、刻み `NEXT_CANDIDATE_STEP` の候補で
  再評価（`NEXT_DISCOUNT` 掛け）。物理が重いので held より粗い
- 埋め込みが主減点。同種ペア待ちを、より大きい異種で直上・肩から塞ぐ手を強く引く
- 異種中央狙い（`FOREIGN_AIM`）: 真下の実が異種かつ中心帯なら減点。真下が同種なら OK。
  `merges` では切らない（異種真上から転がって合体する抜け道を塞ぐ）。谷・肩への異種積み
  そのものは禁じない
- 同種過多（`EXCESS_SAME`）: 同種が 3 個以上たまったら超過 1 個あたり 20 の減点
- 谷育成（`valley_grow_ok`）: 谷の実が held と同種、または谷の実が held のひとつ上で
  held と next が同種なら、その谷への着地にボーナス。基準は谷の実で壁の型は見ない
- レイアウト: 大実どうしは近接。大側（`sign`）で端に付いた L の外側かつ L 中心より下の
  角ポケットを強く減点（`_big_layout_penalty`）
- ただし隣り合う 2 つの間に型が抜けているなら、そのぶんの隙間は詰めない。詰めると
  抜けている型をツモったとき置き場が無く、外側へ回して並び順を壊すしかなくなる
- 持たないもの: 押し込み合成、復元押し、連鎖隙間空け、一段上への強制寄り、梯子発火のハードコード
- 具体手順の UT は増やさない。壊れたら事故防止か観測側を見る
- `CANDIDATE_STEP` は粗くしない。20 にすると危険な山の真上がグリッドに乗って
  `test_avoids_dangerous_tall_stack` が落ちる。速度は先読み側で稼ぐ
- `SLEEP_FRAMES` を削るのは無効。単発の `choose_x` は速くなるが、盤の収まりが変わって
  次手以降が重くなり、エピソード全体ではむしろ遅い（25 で実測）。物理の忠実度
  （`SimEnv` と共有）も落ちる

### 現行ペナルティルール一覧

`eval = score（合成点） - penalties（事故・悪手の減点）`。手ごとの減点は
`_evaluate_drop`（`src/policy.py`）、盤面全体の減点は `board_penalties`
（`src/penalties.py`）で加算される。

**手ごとの減点（`_evaluate_drop`）**

| ルール | 関数 | 内容 | 重み |
|---|---|---|---|
| 異種ガチ真上 | `foreign_aim_penalty` | 落下列の真下（中心ズレ±20%以内）が異種のとき | 固定 100.0 |
| 谷育成ボーナス | `valley_grow_ok` | 谷の実が held と同種／谷の実が held のひとつ上で held と next が同種、その谷への着地 | **−3.0**（この表で唯一のボーナス） |
| 中央寄りタイブレーク | `center_tiebreak` | 落下列と中央の距離。**順位を決めるだけの項**で、手の良し悪しを表さない | `CENTER_TIEBREAK_WEIGHT` 0.001（最大 0.19 ＜ 最小合成点 1.0） |

谷育成ボーナスは **held 自身が合体しなかったときだけ**掛かる（`held_merged`。合体の有無
`merges` ではない。盤の別の場所で無関係な合体が起きただけで免除されないように）。

**盤面全体の減点（`board_penalties`、落下後の盤に対して毎回）**

| ルール | 関数 | 内容 | 重み |
|---|---|---|---|
| 埋め込み度 | `_bury_penalty` | 合成候補の実が異種で覆われている度合い（兄弟あり1.0／なし0.35）。接触の窓は**両方の半径**基準 `(under.radius + over.radius) × 0.9`（下の実だけを基準にすると、大きい実が小さい実に乗るほど窓が狭まり検出から外れる） | `BURY_WEIGHT` 20.0 倍 |
| 肩乗り | `_perch_penalty` | 大実（最大実から `PERCH_BIG_SPAN` 1 段下まで）の footprint の中で、下端がその大実の中心より上にある小実。型差が `PERCH_MIN_GAP` 5 を超えた分だけ数える（パインの肩にオレンジまでは 0、デコポン1／ぶどう2／いちご3／さくらんぼ4）。接触は要求しないので、間に 1 段挟んで山の上に乗った形も拾う | `PERCH_WEIGHT` 16.0 倍 |
| 同種過多 | `_excess_same_penalty` | 同種が3個以上（2個までは合成待ちとして許容） | 超過1個につき 20.0 |
| 大小順逆転 | `_size_order_penalty` | 左右で大小が逆転しているペア（より大きい実の谷にはまっていて、**かつ盤に同種の相方が残っている**実だけ対象外＝`_size_order_exempt`）。**held が合体した手では免除** | ペア差×1.5 ＋ ideal_x乖離×0.004 |
| 角ポケット | `_corner_pocket_penalty` | 最大実が大側壁に付いているのに、その外側・下に小実がある（L の裏に入った実は相方に会えない） | 50.0×(1+0.05×型差)＋depth×0.15 |

**減点ではないもの: 致死手フィルタ（`choose_x`）**

落下後の盤が負けライン（`GAME_OVER_Y` 14.9）を越える候補は、生存候補が 1 本でも
あるなら eval を比べる前に捨てる。減点ではないので上の表には無い
（→[危険な高さの傾斜をフィルタに置き換えた](#危険な高さの傾斜をフィルタに置き換えた-2026-08-20)）。

補足:
- 上のルールに ON/OFF トグルは無い（恒久化した規則のトグルは残さない方針。
  A/B の手順は [AGENTS.md](AGENTS.md#方策学習を触るとき)）
- 梯子検出（`src/ladder.py`）は現状ペナルティには未使用（検出のみ）

### 実行コスト: 物理の高速化と探索幅 (2026-08-17)

探索コストはほぼ `simulate_drop` の呼び出し数。`HELD_TOP` / `NEXT_CANDIDATE_STEP` が
実行時間を決める。

**物理の高速化: 単価を 9.42ms → 3.86ms（2.44 倍）にした。** play は完全に不変なので
score では測っていない。`choose_x` の 99.2% が `simulate_drop`（1 手 72 回）で、
その中身の **57.9% が `_find_merge_pair`** だった。10 実の盤で 1 ドロップ 55,104 回
呼ばれて**戻り値は 100% None**。`_MergeScan`（一番近い同種ペアの隙間と最大速度から
「あと何サブステップは接触しえないか」を出して走査を飛ばす）で 55,104 → 3,717 回に
なり 4.43ms、`_all_quiet` を後ろから見る等で 3.86ms。

- 隙間が閉じる時間の下限は、速度が重力でしか増えない（`elasticity=0`）ことから
  `g*T² + 2vT = gap` の正の解で出る。押し出し補正は `body.velocity` に出ないので、
  速度側に 60px/s の下駄（`SCAN_SPEED_MARGIN`）と 16 サブステップの上限を掛けてある
- **検証のしかた（この種の変更では score を測らない）**: 各手のあとの全実の x / y を
  `repr()` で、選んだ x・score・合成回数と一緒に記録し、master の worktree の出力と
  **バイト比較**する。3 seed × 70 手と 2 seed × 170 手が全て一致。スキップ幅が
  1 サブステップでも長すぎれば合体がズレて以降の軌道が全部変わるので、敏感な検査になる
- 残りの伸びしろは小さい（物理 ~62% / 静穏ゲート 18.5% / 走査 10.8% / 組み立て 5.9%）

**探索幅 8/16 は再測定して採用しなかった。** 一致率スクリーニング 210 局面で
一致 75.2%（4 手に 1 手は別の手）だったので A/B に進んだ。n=100（2.4 時間、打ち切り 0 本）で
score 2105.7 → 2185.3（**+79.6 / +3.8%**、t=1.68、CI [-14.3, +173.5]）。**有意でない**が
**9 指標すべてが B 側に振れ**、`cascades` +6.6% だけは有意（t=2.36）。
シード対戦 win 59 / loss 41。有意にするには n≈136。

- 点推定 +3.8% は、旧記録の「2/32 で −3.4%」と**独立な測定でほぼ一致**した
- 分位点どうしの直接比較では**下を持ち上げて上を削っている**（最小 1148→1499、
  中央 2076→2179、最大 3489→3215）。ただし type10 到達は 12→20 本と増えている
- **採用しなかった理由**: 1 手 233ms → 856ms（**3.68 倍**）に対して +3.8%。
  教師収集（`train_sim.py`）が丸ごと 3.68 倍高くつく。**否定された案ではない**ので、
  もっと安い先読みが書けたら再検討する価値はある
- **この順番（数分のスクリーニング → 数時間の A/B）は次からも踏むこと**

## 学習まわり

- **score / penalties**: `score` は本家の合成点（1〜65、減点なし）、`penalties` は事故・悪手の減点。
  `eval = score - penalties` は **bootstrap の手選び専用**（`choose_x`）。生徒の良し悪し・保存・
  ログは本家点 `score`（同点なら手数）。**RL の報酬も score**（密な減点は報酬にしない）
- `src/reward.py`: `merge_score(merge_types)` が本家と同じ合成点のみ（cherry→0 …
  watermelon 55、ダブル消去 65）。生存加点・死亡減点なし
- `src/training/encode.py`: 固定長観測ベクトル
- `src/sim/sim_env.py`: 画面なし落下 sim。`SimStep` は本家点 `score` のみ
- 評価: `python scripts/eval_policy.py`（`--policy bootstrap|learned`。`--workers` 既定=論理コア/2）
- A/B: `python scripts/compare_policy.py`。**比較したい変更は `_apply_variant` に差す**
  （空だと A と B が同じになり、全シード tie の警告が出る）。1 本でも `max_steps`
  打ち切りがあれば警告。`--seed` は省略でランダム。`--out` で生データを JSON 保存
- 代理指標の探索: `python scripts/analyze_ab.py <dump.json>`
- 足切り: `python scripts/band_escape.py`（→[足切りは「帯の外へ出るか」で測る](#足切りは帯の外へ出るかで測る)）
- 統計は `src/util/stats.py`（ペア差の t / 95% CI / 必要 n / 相関）。scipy は入れていない
- 学習: `python scripts/train_sim.py`（収集 → オフライン BC。既定 max-steps=300 は打ち切りであり
  負けラインではない）。best は score → 手数 → match
- 教師収集は `ProcessPool` 並列（既定 workers=論理コア/2。9700X なら 8。`--workers 1` で直列）
- `src/training/agent.py`: 離散列 32 ビン / hidden 128 の MLP
- 実プレイ: `python main.py`（npz があれば既定 learned。`L` で bootstrap 切替、`--policy bootstrap`）

### 調査済み: BC が match 60-70% に届かない (2026-08-05)

既存チェックポイントを計測したところ score は bootstrap (~2000〜2150) の半分程度
(~1040〜1060) しか出ず、RL 着手の条件に遠く届いていなかった。

**見つけたバグ (修正済み)**: `src/training/encode.py` の `MAX_FRUITS` が 16 で、盤面の実を
大きい型から優先して詰め、超過分を切り捨てていた。終盤は 20 個超の盤も珍しくなく
(実測 23 個)、真っ先に切り捨てられるのは cherry/strawberry —
[終盤の低段位散在による即死](#調査済み-終盤の低段位散在による即死)で特定した事故の
張本人が生徒から見えていなかった。32 に上げて実質切り捨てなしにした。

**それでも解決しなかった**: 教師データ (150 ep, n=32562) を固定して
lr 0.05〜1.0 × epoch 80〜300 × hidden 128/256 を総当たりしても match 30% に届かず
（最良: hard label, lr=0.5, hidden=256, epoch=300 で 29.1%）、score も 900〜1150 で頭打ち。
soft label 方式でもむしろ低い (15〜21%)。

**分かったこと**: bootstrap は候補ごとに実際に `simulate_drop` を回して結果を比べる方策で、
`encode.py` の静的な特徴量（実の type/x/y/r の羅列）だけから 1 隠れ層 MLP がその判断を
真似るのは、学習率・エポック数・soft/hard のどれを振っても頭打ちになった。
**表現力か特徴量設計（候補ごとの着地結果を特徴に含める）自体を変えない限り届かない。**

**同点帯を考えても足りない (2026-08-19)**: 教師が帯の中ででたらめに選んでいる以上、
厳密一致で測る `match` は生徒の表現力と無関係に落ちる。帯一致で測り直すと（帯 eps=0.1、
行動 32 ビン、**でたらめ撃ちの対照つき**）全体 10.1% → **33.6%**。だが対照が 19.8% ある
土俵なので **1.7 倍**にすぎない。しかも対照比は early 2.4 倍 → **late 1.4 倍**で、
**盤が決まる区間で生徒はほぼでたらめ**。門の目盛りは確かに厳しすぎたが、
「教師を真似られている」水準ではない。
（留保: 使ったのは hidden=128 のチェックポイントで、掃引の最良ではない）

## 予定: RL (REINFORCE)

- いまはまだ早い。BC が浅いと素の REINFORCE は壊しやすい（過去に確認済み）
- 足す条件: `match` が高め（目安 60–70%+）かつ生徒の `score` が bootstrap に近い
- やり方: BC 完了後に短い微調整だけ（例: `--episodes 50 --lr 0.002`）。既定はオフのまま
- それまでは BC（収集量・epoch）を厚くする方が先
